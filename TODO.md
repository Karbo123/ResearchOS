# Research OS TODO

最后更新：2026-08-04（Asia/Shanghai）

状态只使用：`[ ]` 待处理、`[~]` 进行中、`[x]` 已完成且已验证、`[!]` 外部阻塞。

本文件是 Research OS 的实现合同和验收清单，不是宣传文案。只有代码、数据契约、测试、文档和真实适用验证都完成，任务才能标为 `[x]`。fixture、静态页面、数据库表、模型建议、旧缓存或无关实验都不能冒充真实能力。任何大模型、来源 API、Supermemory、GPU 或 LaTeX 调用失败，都必须保留结构化失败；禁止本地回复、换 provider、空数组、旧缓存或无关实验等 fallback。

## 排序原则（2026-08-03 用户再次强调）

1. **当前唯一优先：`P0-WORKSPACE-108A` 至 `108H`。** 这是用户刚发来的项目工作台重构，必须严格按顺序开发，旧 TODO 不得插队。
2. **之前的 TODO 统一收进“旧任务池”。** 它们仍然保留在本文档中，但明确是非当前优先；只有新工作台完成并通过验收后，才重新评估。
3. **外部阻塞单独列出。** 等待外部环境或人工操作才能继续，不代表可以跳过或伪造通过。
4. **新工作台内部也先做可见界面。** 每个 `108*` 步骤先完成用户能直接看到、点击、操作的页面和交互，再补真实 API、状态、审批与验收。

## 全局设计契约：Apple 设计风格（适用于每一个 TODO item）

Research OS 的视觉与交互参考 Apple 设计规范，并落实到本文档中的每一个 TODO item。任何页面、组件、状态、动效和验收截图都必须体现以下原则：

- 材质：优先使用磨砂玻璃、玻璃质感、半透明和 `backdrop-filter` 模糊分层；面板、侧栏、弹窗、工具条不应是生硬的不透明色块。
- 动效：所有显示/隐藏、抽屉滑入滑出、标签切换、悬浮、点击、滚动、加载和状态变化都应有平滑的动态效果；推荐使用 Apple 式的弹簧曲线和缓动，避免瞬跳、突兀出现或无过渡。
- 阴影与层次：使用柔和、分层、有方向的阴影表达层级和景深，避免生硬描边和厚重黑边；半透明材质下也要保证文字对比度和可读性。
- 视觉细节：保持充足留白、克制的圆角、清晰的字体层级、语义化主题颜色、浅色/暗色两套主题；不写死颜色，不使用与整体主题冲突的组件。
- 交互：悬浮操作、拖动、标签栏、滚动条、加载和空状态都要符合 Apple 式的轻盈感；键盘焦点、读屏和 `prefers-reduced-motion` 仍必须可用。
- 验收：每一个 TODO item 完成时都要做真实浏览器检查；界面相关项必须截图确认磨砂、半透明、动效、阴影、明暗主题和窄屏/移动端效果，不能只验证逻辑。

本文档每个 TODO item 末尾的 `[Apple 设计验收]` 表示该条目必须同时满足上面的全局设计契约，不能只完成功能就关闭。

## 1. 用户原始目标（唯一解释源）

> 2026-08-03 最高优先级需求变更：Research OS 从“首页先讨论 Idea、项目内按技术功能切换许多页面”改为“首页管理全部项目、进入项目后在一个长期工作台内持续研究”。本节的新合同覆盖本文后面仍引用旧导航结构的描述；旧 URL 只做兼容重定向，不能恢复旧界面。

Research OS 要做的是一个本地、可审计的科研工作台：用户给出研究方向并持续审阅关键决定，AI 按顺序协助完成 Idea 澄清、相关工作调研、公开代码复现、我们的方法开发、实验、定期汇报和论文编译。每一步都必须留下来源、版本、审批、失败状态和可追溯的产物，不能把模型的猜测写成科学结论。

### 1.1 产品形态与设计初衷

Research OS 的首页是项目入口，不是 Idea 聊天页。首页像一个科研项目控制台：每一行代表一个项目，用户可以先快速比较项目状态，再进入某个项目的完整工作台。创建项目应像新建文件夹一样轻量，只填写项目缩写和标题即可；Idea 澄清属于项目创建后的研究活动，不能成为创建项目的前置门槛。

进入项目后，页面应尽可能把空间留给当前研究内容：左侧项目列表默认收起，通过边缘箭头平滑滑入或滑出；右侧始终保留唯一的项目级多轮对话。这个对话不是某个标签页的临时聊天，也不做角色扮演，它共享同一份项目对话历史，可以讨论 Idea、文献、实验、审批、论文或一般问题，并根据当前页面获得明确、可审计的上下文。

四个一级入口仍固定为 `项目概述 -> 相关工作调研 -> 实验实现 -> 学术论文撰写`，但二级信息架构改为用户的科研任务，而不是内部数据表、技术模块或零散工具。前面三个区域产生的已确认内容、证据和实验结果，最终服务于论文撰写；任何跨区域引用都必须保留来源和版本，不能把模型建议当作事实。

### 1.2 页面信息架构合同

| 页面层级 | 固定内容 | 设计目的 |
| --- | --- | --- |
| 首页 | 项目表格/列表、项目状态摘要、创建项目入口 | 先看全局，再进入单个项目；首页不显示项目对话 |
| 项目外壳 | 可收起的项目抽屉、项目标题与全局状态、四个一级入口、常驻项目对话 | 最大化主内容空间，并保证跨页面讨论不断线 |
| 项目概述 | `项目总览`、`Idea 讨论`、`待审批与决策`、`定期汇报与反馈` | 汇总项目事实、创新讨论、需要用户处理的动作和长期进展 |
| 相关工作调研 | `文献列表`、`研究可视化`、`种子文献扩展` | 先审阅全部文献，再理解时间/引用脉络，并可从种子继续扩展 |
| 实验实现 | `本方法实现`、`相关工作实现` | 用同一套实验管理范式分别管理自己的方法和前人方法；不再使用第三级标签栏 |
| 学术论文撰写 | `引言`、`相关工作`、`方法介绍`、`实验`、`结论` | 按论文内容结构组织 AI 写作，而不是按编译、引用等内部工具拆页 |

项目概述的职责：

- `项目总览` 汇总研究目标、简要描述、创新点、项目状态、整体进度、实验总数与运行状态、待审批数量、论文进展和最近活动；现有项目规格中适合全局理解的字段迁入这里。
- `Idea 讨论` 合并原 Idea 对话与创新/边界能力，展示经过讨论形成的研究问题、创新候选、边界、重复风险和研究现状矩阵，并支持在矩阵基础上做可审阅的排列组合探索，找出尚未覆盖的创新候选；创新只能是有来源、可确认的候选。
- `待审批与决策` 统一承载 AI 提出的 Proposal、需要确认/拒绝/修订的动作和用户指导；原实验页的“变更与审批”迁入这里。
- `定期汇报与反馈` 以真实工作流事件汇总研究、实验和论文进度，支持用户反馈并保留来源快照，不在前端使用“导师/老师”等角色词汇。

相关工作调研的职责：

- `文献列表` 每行展示标题、作者、机构、年份、引用量、来源、全文/下载链接、项目主页和明确的开源代码仓库；未知字段如实显示，不能猜测。
- `研究可视化` 提供按年份/时间线的文献分布和引用关系图；节点大小可按真实引用量映射，边只表示已保存的引用关系，节点可打开文献详情。
- `种子文献扩展` 接受 DOI、标题、URL、BibTeX、PDF 或已有 Paper，从其参考文献与后续引用工作双向扩展，形成可审阅的研究脉络；搜索同时尽可能收集论文明确提供的代码与项目主页，为后续复现提供候选。

实验实现的职责：

- `本方法实现` 和 `相关工作实现` 共用“左侧实验列表 + 右侧实验详情”的稳定布局，不再出现第三级横向标签栏。列表同时显示待做、等待审批、排队、运行、完成、失败和取消的实验。
- 详情区展示实验目的、设计依据、验证目标、方法概述、计划、状态和指标；提供类似文件浏览器的代码工作区，显示项目内受控文件树、Git 状态/提交、代码、配置和结果。
- 结果区域支持图片、曲线、CSV/JSON、PDF、点云和 3D mesh 等常见 Artifact 的真实预览，并展示运行队列、seed、指标统计和谱系，以覆盖常见 TensorBoard 式查看需求。
- 本方法实验可以由项目对话提出，也可以由 AI 分析后创建待审批 Proposal；相关工作实现必须从已确认 Paper 和明确仓库 URL 开始，无代码时可通过项目对话讨论复现方案。两者都不能绕过审批、安全、Git 和 Artifact 门禁。

学术论文撰写的职责：

- 二级标签固定为 `引言 -> 相关工作 -> 方法介绍 -> 实验 -> 结论`。每一节只消费允许引用的项目概述、文献证据和实验 Artifact，并显示句子、引用、图表和数据的来源。
- `引言` 引用项目总览中的研究动机/目标、相关工作调研中的已有工作和实验结果中的指标；`相关工作` 只引用已确认文献证据和项目概述，说明本工作与前人工作的关系和改进；`方法介绍` 基于实验设计和项目概述说明方法；`实验` 只呈现真实实验、消融、对比和数据集；`结论` 写研究结论、局限和未来工作。
- AI 负责生成和修订英文论文；每个英文句子在编辑/审阅界面提供对应中文翻译，帮助中文用户理解，但中文译文不得写入最终 PDF。
- AI 完成生成或修订后，系统自动调用 Linux `latexmk` 编译并展示最终 PDF 或编译错误；用户可在右侧项目对话中继续指导章节修改、图表绘制与插入位置、流程图和整体可视化。
- 默认使用 CVPR LaTeX 模板和 Linux `latexmk` 编译；展示编译状态、错误和最终 PDF。论文源码、引用、图表和编译配置使用独立于实验代码的项目内 Git 历史。
- 项目对话始终位于右侧，可讨论章节修改、图表绘制和插入位置、补充实验、流程图与整体可视化；对话建议必须转成可审阅变更，不能直接覆盖已确认稿件或伪造素材。

### 1.3 端到端用户顺序

1. 在首页浏览全部项目的实验、审批、研究和论文进展；需要时只用项目缩写和标题创建一个空项目。
2. 点击项目进入工作台，默认打开 `项目概述 -> 项目总览`；项目列表收起，右侧项目对话保持常驻。
3. 在 `Idea 讨论` 或常驻项目对话中逐步澄清研究问题、目标、边界和创新候选，并审阅结构化 `IdeaVersion`。
4. 在 `相关工作调研` 审阅文献列表与研究可视化，再从用户选择的种子文献扩展前后引用脉络和代码候选。
5. 在 `待审批与决策` 统一处理文献、代码、依赖、实验、Artifact、论文变更等 Proposal；反馈不能直接触发副作用。
6. 在 `实验实现 -> 本方法实现` 管理自己的实验，在 `相关工作实现` 管理前人方法复现；通过实验列表选择工作项，在详情区查看代码、Git、队列、指标、结果和谱系。
7. 在 `定期汇报与反馈` 查看基于真实事件的阶段总结并反馈，系统把反馈转成新的讨论上下文或待审批动作。
8. 在 `学术论文撰写` 按引言、相关工作、方法、实验、结论逐节生成和修订英文论文，同时审阅逐句中文翻译与来源。
9. 使用项目内独立论文 Git 保存版本，以 CVPR 模板编译 LaTeX 并审阅 PDF；失败保留结构化错误，不能显示旧 PDF 冒充成功。

## 2. 不可违反的工程边界

### 2.1 TypeScript 与 Python

- `apps/`、API、数据库迁移、Mastra 集成、worker、运维脚本、验收脚本和测试运行时代码全部使用 TypeScript/TSX。
- React 是 `apps/web/src/` 的唯一 UI 业务源码入口。旧的 `index.html` 只能作为静态挂载壳，不能重新加入原生 HTML 交互。
- Python 仅允许作为科研实验或论文代码复现入口，路径必须位于 `projects/<project-id>/experiment/` 或其受控 reproduction 子目录，并使用该项目自己的 `projects/<project-id>/.venv`。
- Python 实验由 TypeScript supervisor 通过固定入口启动；模型不能提供命令、路径、依赖安装命令、shell、SQL、网络目标或可执行文件。
- 不允许为了“方便”把 Python 引擎、旧 `requirements.txt`、旧 SQLite、旧 JSON cache 或旧项目脚本加入应用运行路径。

### 2.2 WSL2 运行边界

- 完整应用栈只在 WSL2 Ubuntu 22.04 内运行：API `127.0.0.1:8080`、Mastra `127.0.0.1:4111`、Supermemory `127.0.0.1:6767` 及配置池端口。
- 开发、编辑、构建、测试和运维命令全部在 WSL2 shell 中执行；默认开发 shell 是 WSL2，不使用 Windows cmd/PowerShell。
- Windows 只作为调试浏览器客户端：固定使用 Windows Chrome，通过 mirrored 网络/端口转发以 `http://127.0.0.1:<port>` 访问 WSL2；不恢复 Windows 原生应用运行目标，不依赖 Docker。
- 当前唯一代码副本是 `/mnt/d/ResearchOS`，对应 Windows 的 `D:\ResearchOS`；禁止复制一份再同步导致双份事实源。
- 实验、Supermemory、Mastra、API、LaTeX 和数据库都在 WSL2 进程内运行；Windows Defender 只作为上传扫描的受控互操作依赖。
- 所有监听器默认只绑定回环地址。`0.0.0.0` 不能作为产品默认监听地址。

### 2.3 模型、Mastra 与失败关闭

- Luna、Terra、Sol 三档的 model、URL、key 和 reasoning effort 完全独立；读取 API 只能返回 `key_configured`，不能返回 key。
- Idea 澄清、项目监督和实验规划必须使用 Mastra Agent/Skills/Tools/Workflows；不能手写 Agent 循环或把提示词复制成另一套 Agent 实现。
- Mastra 负责模型推理和审批编排；确定性 HTTP adapter、Zod 校验、递归队列、去重、排序、数据库写入、指标统计、Artifact 校验和 lineage 检查由 TypeScript 服务负责。
- Agent 不得获得任意 Shell、SQL、文件路径、可执行程序或网络工具。模型输出不能直接成为命令、路径、SQL、依赖、URL、实验数字或论文事实。
- 模型配置错误、HTTP 失败、超时、鉴权失败、无效结构化输出、Supermemory 失败和来源失败都返回结构化错误；不本地回答、不隐式换 provider、不静默重试成成功、不写入伪造助手消息。
- 固定工作流任务的重启/恢复与模型 fallback 是两件事：队列可以恢复确定性状态，但不能用另一个模型或规则逻辑代替失败的模型请求。

### 2.4 Supermemory 与项目隔离

- Supermemory 是项目范围的语义 Memory、向量检索、Graph Memory 和 Super RAG；PGlite 只保存结构化状态、权限、实体 ID、审批、hash 和审计。
- 每条 memory 必须带不可变 `project_id` scope/container/resource、来源 Artifact、SHA-256、页码/章节、Idea 版本、实验/报告 ID 和证据状态。
- 相同 embedding provider/model/dimensions/base URL/key 的项目可以共享一个 Supermemory 配置池实例；项目隔离仍必须由 Supermemory scope 和本地权限同时保证。不同 embedding 配置必须使用不同池和不同数据目录。
- embedding 模型/维度切换必须新建数据目录或完整重索引，不能把不同向量空间混在同一个索引里。
- Supermemory 失败、超时、鉴权失败或返回无效数据时，直接返回结构化错误，不能改用 SQL 词法检索、另一 provider、本地旧缓存或无关实验。
- 当前外部阻塞依照 `P0-SUPERMEMORY-LOCAL-053` 记录，不能把 delete 验证写成 forget 验证，也不能把 fixture acceptance 写成真实图片/PDF 通过。

## 3. 旧项目 `/mnt/d/auto-related-work` 的复用边界

### 3.0 可阅读、可复用与迁移原则

- 允许阅读 `/mnt/d/auto-related-work`（即 Windows `D:\auto-related-work`）中的 README、`backend/`、`scripts/`、`tests/` 和已有分析文档，作为相关工作自动找论文的设计参考。
- 允许复用其中的算法、字段 schema、缓存策略、测试思路和数据处理设计；“复用代码”指把可用逻辑翻译成 TypeScript 并纳入 Research OS，不能直接 `import`/`exec`/调用 Python 文件，也不能把 Python 运行时或旧缓存作为依赖。
- 自动找论文、多来源检索、引用递归、标题/作者/DOI 归一化、字段补全、去重、请求缓存、阶段进度和结构化失败都应按上述方式迁移为 TypeScript；旧项目中的 `.env`、key、Cookie、代理、Scholar 爬虫隧道和旧数据库/缓存不得带入。

### 3.1 绝对禁止

- 不直接 `import`、`exec`、`child_process` 调用、symlink、npm workspace 引用、构建打包或运行时读取旧项目的 Python 文件；等价逻辑只能以 TypeScript 重写后使用。
- 不复制旧项目的 `.env`、key、Cookie、代理、SQLite/WAL、JSON cache、最终 JSON、HTML 交互或 `requirements.txt`。
- 不把旧项目的固定 topic、旧实验数字、旧爬取数据库、旧进度文件或旧日志当作当前项目事实。
- 不迁移 Google Scholar HTML/Cookie/住宅代理/CAPTCHA 隧道和无界全库爬取。
- 旧项目本身是用户的参考资料，不因为迁移工作而删除；删除旧目录不会等于完成 TypeScript 重写。

### 3.2 已归档台账

旧项目的 49 个 Git 跟踪文件逐项处理台账、按能力拆分的 TypeScript 重写路线和“保留价值 / 必须改变”对照表已经完成并归档（保留在 git 历史中）。归档内容全部落实为当前仓库的 TypeScript 实现与淘汰决定：多来源搜索与字段补全、作者/标题/DOI 归一化、引用递归、请求缓存、阶段进度、结构化失败和 UI 页面都已重写；Scholar 爬取、Cookie、住宅代理、CAPTCHA、百万关键词、全库填充和旧进度文件全部淘汰。下面的 `AUTO-TS-*` 是迁移完成后的真实验收项，不能用 fixture、旧缓存或旧结果代替。

### 3.3 迁移验收与排除清单

- [ ] `AUTO-TS-01` 用一条用户提供的真实 seed 走通搜索，不调用旧项目 CLI、不加载旧缓存、不运行 `python`，并在网页上展示每次来源尝试的进度和结果。 [Apple 设计验收]
- [ ] `AUTO-TS-02` 对同一篇论文验收多来源字段冲突、用户选择、字段来源标记和证据状态；没有 PDF 原文 quote 的候选不能进入已确认矩阵。 [Apple 设计验收]
- [ ] `AUTO-TS-03` 验收至少两层引用递归，包含去重、宽度/总量上限、取消、部分结果和失败；确认 DBLP/arXiv 的“只搜索、不支持引用递归”能力在界面上如实说明。 [Apple 设计验收]
- [ ] `AUTO-TS-04` 验收缓存命中、过期、schema 不兼容、缓存损坏、失败不覆盖成功、取消不写缓存和两个项目互不串数据；旧 SQLite/WAL/JSON 缓存不得被读取。 [Apple 设计验收]
- [ ] `AUTO-TS-05` 验收论文明确给出的 GitHub/GitLab 固定 commit 复现和 TypeScript 比较；复现源码只能进入 `experiment/reproductions/`，不得进入方法代码或方法 Git。 [Apple 设计验收]
- [ ] `AUTO-TS-06` 运行 `scripts/check-language-boundary.ts` 并检查构建产物、依赖图和运行脚本；旧 Python 只能出现在项目科研实验/复现目录，不能成为应用服务依赖。 [Apple 设计验收]

## 4. 待办任务（当前以 108A-H 为唯一优先）

### 4.0.0 当前最高优先级：视觉巡检反馈修复与项目级设置（2026-08-04）

> 2026-08-04 用户明确要求：拿到完整视觉反馈结果后，先把这些结果写入 TODO，再继续修复全部问题，并列为最高优先级。以下 `P0-VISUAL-116` 至 `P0-VISUAL-120` 对应 `runtime/visual-audit-20260804/vision_results.md` 中 79 张真实浏览器截图的 AI 视觉评审；`P0-SETTINGS-121` 对应“设置除外观外全部项目级、配置面板改为 通用/模型/系统”的最新需求。完成后需重新跑完整浏览器验收，不能只修静态样式。

> 2026-08-04 修复批次：已为首页/侧栏/实验/论文列表补齐 `title` 提示，长 ID/URL 启用 `overflow-wrap:anywhere`；统一 hover/active/focus-visible 与状态色语义；收紧 404 页（进度条、立即返回提示、对比度）；修复聊天输入区裁剪、徽章重叠、论文按钮截断和设置二级标签截断；新增项目级设置单元测试。第三轮 79 张截图复验（`runtime/visual-audit-20260804-final/`）后继续修复：`.project-layout` 与 `.new-view` 固定 `grid-template-rows:minmax(0,1fr)` 根因，聊天面板在窄桌面 761–1050px 改为可折叠浮层且不再遮挡内容，设置弹窗“配置/外观”副标题去重，审批“影响:{}”改为友好文案，深色设置选中态使用蓝色文字/描边；第四轮 5 张针对性截图复验通过（`runtime/visual-audit-20260804-final2/`）。

- [x] `P0-VISUAL-116` 修复项目名称/URL/ID 截断与信息完整性：首页项目行、左侧项目列表的长标题不能被无提示截断，至少提供 `title`/`aria-label` 完整信息、可读的换行或可展开机制；长项目 scope ID、仓库 URL、论文状态文本等使用 `overflow-wrap:anywhere` 或明确省略样式，不允许生硬裁剪或破坏布局。 [Apple 设计验收]（已补首页/侧栏/实验/论文 `title` 与 `overflow-wrap:anywhere`；首页/侧栏长标题使用省略号+`title` 是刻意行为，第三/四轮真实截图复验确认正文不再生硬裁剪）
- [x] `P0-VISUAL-117` 补齐交互反馈与颜色对比度：所有可点击行/按钮/图标必须有明确的 hover、active、focus-visible 状态；状态徽标（Active/Cancelled/Pending/succeeded/failed 等）、灰色辅助文字在浅/暗主题下达到 Apple 风格的 4.5:1 对比度；统一成功=绿、失败/取消=红、等待=琥珀、信息=蓝的语义，不使用冲突色。 [Apple 设计验收]（已统一状态色、加深浅色 `--muted`、补 focus 描边、深色设置选中态使用蓝字/描边；关键文字按 WCAG 公式验证通过，剩余评审意见为“标签柔和度/图标风格”等主观建议）
- [x] `P0-VISUAL-118` 统一间距、对齐与密度：首页统计卡、表格列宽、顶部状态栏、标签栏、卡片内边距、右侧项目聊天输入区在桌面/窄桌面/移动端都有稳定留白与视觉基准；长内容底部不裁切，聊天输入框、按钮与提示完整可见；状态徽章不重叠、不挤占触控目标。 [Apple 设计验收]（根因修复 `.project-layout` 行高约束，composer/seed 控件不再被裁；窄桌面聊天改为可折叠浮层，移动端 toggle 不再遮挡；DOM bounding rect 与第四轮截图复验通过）
- [x] `P0-VISUAL-119` 打磨 404 页：当前地址文字与背景对比度提升，倒计时增加进度/动态反馈并说明点击可立即返回；移除或弱化冗余地址输入框观感；图标与系统线性图标统一，间距、视觉层级按反馈收紧。 [Apple 设计验收]（已加进度条与返回提示、强化路径文字对比、按钮 `inline-flex` 不换行；第三轮四语言/暗色/移动 404 截图复验通过）
- [x] `P0-VISUAL-120` 处理工作区细节反馈：论文“AI translate/revise”等按钮不截断，章节列表、代码复现卡片、状态参考面板、移动端二级标签对齐清晰；防止长 ID、长 URL、长文本溢出；保留 Apple 玻璃材质、半透明和柔和阴影的同时提升可读性。 [Apple 设计验收]（已让论文按钮换行、设置二级标签不截断、长文本可换行、seed expansion 控件完整可达；第三/四轮截图复验确认无结构性截断）
- [x] `P0-SETTINGS-121` 左下角设置改为项目级：界面语言与主题保持全局（所有项目共享），其余设置（代码三档、文档文本、图片识别、图片生成、Embedding、语音识别）全部按项目隔离并随项目切换；配置面板一级标签固定为 `通用 -> 模型 -> 系统`，通用只放外观，模型放全部模型设置，系统放全局代理；模型设置在未打开项目时提示先打开项目，不再允许编辑全局模型配置。 [Apple 设计验收]（服务端/前端项目级隔离、API、Mastra 接线、5 个单元测试与 178 个服务端测试通过；设置弹窗副标题去重后第四轮截图复验通过）
- [x] `P0-VISUAL-122` 修复第三轮残留问题：窄桌面（761–1050px）项目聊天面板改为可折叠浮层，默认不遮挡主内容；设置弹窗“配置/外观”副标题不再重复；审批页空 `impact` 不再显示“影响:{}”，改为四语言“暂无影响记录”；深色主题设置 Segmented Control 与一级标签选中态使用蓝色文字/描边；`.project-layout`/`.new-view` 固定行高约束，长内容滚动不裁切底部。 [Apple 设计验收]（第四轮 5 张针对性截图与 DOM 探针复验通过）

### 4.0.1 视觉巡检反馈登记（2026-08-04，79 张真实浏览器截图）

完整 AI 视觉评审原文保存在：

- 首轮：`runtime/visual-audit-20260804/vision_results.md`（1121 行，逐张截图给出问题与总体评价）。
- 修复后复验：`runtime/visual-audit-20260804-fixed/vision_results.md`（79 张全部重新评审）。
- 第三轮：`runtime/visual-audit-20260804-final/vision_results.md`（79 张全部重新评审，0 失败）。
- 第四轮：`runtime/visual-audit-20260804-final2/vision_results.md`（5 张针对性复验，0 失败）。

所有反馈已按主题归集如下，修复状态由 `P0-VISUAL-116` 至 `P0-VISUAL-122` 跟踪；同一问题反复出现在多张截图时不再逐张重复。第三轮后仍被 AI 评审提及的剩余意见主要是：状态标签饱和度高/低对比度的主观建议（计算值已满足 WCAG AA）、图标线条风格统一性、玻璃模糊强度的感知差异；这些不影响功能与可读性，已作为后续打磨候选，不阻塞本轮验收。

- 文本截断与信息完整性：首页项目表、左侧项目列表、顶部项目标题、长 scope ID、仓库 URL、论文标题/按钮、`Innovation candidates` 卡片、删除弹窗项目标题、文献作者列表和 UUID 等，必须至少提供 `title`/`aria-label` 或可读换行。首页/侧栏项目名使用省略号加 `title` 是刻意行为，避免长标题挤坏行高；其余正文不允许生硬裁剪。
- 对比度与可读性：状态徽章（Active/Cancelled/Pending、succeeded/failed 等）、灰色辅助文字、聊天输入框占位符、404 当前地址、`Processing` 状态、报告状态标签、文献次要信息在浅/暗主题下都要达到 Apple 风格的 4.5:1 对比度；占位符不得使用接近背景的浅灰。
- 间距、对齐与密度：首页统计卡、表格列宽、顶部状态栏、标签栏、卡片内边距、聊天输入区、设置弹窗在桌面/窄桌面/移动端保持稳定留白；长内容底部不裁切，浮动聊天按钮不遮挡内容，状态徽章不重叠、不挤占触控目标。
- 交互反馈与焦点：所有可点击行、按钮、图标必须有 hover、active、focus-visible；设置 Segmented Control 与标签页的选中态必须比未选中态更明确（蓝色文字/加粗/描边/滑动指示器）。
- 状态语义与配色：统一成功=绿、失败/取消=红、等待=琥珀、信息=蓝；不与整体柔和的玻璃配色冲突，高饱和色只用于关键警告。
- 404 页：倒计时需要进度与动态反馈，地址文字对比度足够，按钮图标与文字间距合理且不换行，避免“返回首页”被挤成残缺文案。
- 移动端与响应式：底部输入区、安全区、双层标签导航、横向滚动标签、浮动项目对话按钮和窄屏统计卡都要完整可见；不允许聊天输入框被裁切或与主内容重叠。
- 玻璃拟态与层次：卡片、侧栏、弹窗保持半透明模糊、柔和分层阴影与高光；避免纯白扁平块；深色主题下边框/阴影仍需可见。

### 4.0 当前唯一优先：项目工作台重构（2026-08-03，必须严格按顺序）

> 设计初衷：先用首页管理全局，再把每个项目变成一个可长期停留的科研工作台；主内容应获得最大空间，项目级对话应跨页面持续存在，导航应反映科研任务而不是内部技术模块。以下 `P0-WORKSPACE-108A` 至 `108H` 是当前最高优先级，覆盖与之冲突的 `070-A2`、`078o`、`078j`、`078l`、旧首页 Idea 流程和旧论文/实验标签合同。必须按字母顺序开发和验收，不能因为后端已有某个接口就跳过前面的可见界面。

> 复查记录（2026-08-03）：已逐字对照最新需求附件，确认 `108A-H` 已完整覆盖首页项目控制台、唯一常驻项目对话、项目概述/相关工作/实验/论文新标签、无第三级实验标签、论文自动编译与逐句中译等内容；旧任务池中冲突条目已改写或删除，最新需求保持最高优先级。

- [x] `P0-WORKSPACE-108A` 固化新信息架构与迁移边界。更新 `TODO.md`、导航类型、URL 语义、旧链接映射和页面状态契约；列出旧 Tab 到新页面的唯一映射，删除重复入口，保证旧深链接只重定向而不恢复旧界面。验收时首页、项目总览和四个一级入口的默认地址唯一且可读，刷新/前进/后退不产生两份状态；页面切换使用 Apple 式平滑转场，导航和状态层采用磨砂玻璃、半透明背景与柔和阴影。 [Apple 设计验收]

旧 Tab 到新页面的唯一映射（108A 已固化为 `navigation.ts` 与 `scripts/check-navigation.ts`）：

| 旧入口 | 新入口 |
| --- | --- |
| `overview_spec`、`overview_innovation` | 项目概述 → `Idea 讨论` |
| `overview_progress` | 项目概述 → `项目总览` |
| `daily_reports`、`weekly_reports`、`feedback_inbox`、`feedback_audit`、`reports` | 项目概述 → `定期汇报与反馈` |
| `literature` | 相关工作调研 → `文献列表` |
| `research_status`、`citation_graph` | 相关工作调研 → `研究可视化` |
| `method_design`、`code_workspace`、`policies`、`experiments`、`experiment_queue`、`experiment_metrics`、`artifacts`、`lineage` | 实验实现 → `本方法实现` |
| `reproduction`、`comparison` | 实验实现 → `相关工作实现` |
| `paper`、`paper_outline` | 学术论文撰写 → `引言` |
| `paper_citations` | 学术论文撰写 → `相关工作` |
| `paper_figures` | 学术论文撰写 → `方法介绍` |
| `paper_data` | 学术论文撰写 → `实验` |
| `paper_compile`、`paper_review` | 学术论文撰写 → `结论` |
- [x] `P0-WORKSPACE-108B` 重做首页为“全部项目”控制台。移除首页 Idea 多轮对话；使用可扫描的项目行展示项目标题/缩写、状态、实验总数与运行/完成数、待审批数、文献进展、论文进展和最近更新时间；支持置顶、拖动排序、删除与进入项目。创建项目改为轻量表单，只要求项目缩写和标题，像新建文件夹一样立即创建；缩写执行项目 slug 的字符、长度和去重校验，不调用模型、不要求先讨论 Idea。空、加载、失败、无项目、窄屏和键盘操作均需真实浏览器验收；首页项目行、创建表单和弹窗采用 Apple 风格毛玻璃卡片、半透明面板、柔和阴影与流畅的悬浮/按压/状态动效。 [Apple 设计验收]

> 验收记录（2026-08-04）：首页项目控制台已通过桌面 1440 与移动 390 的真实浏览器检查，无横向溢出；四语言、暗色主题、创建项目、进入项目、置顶、删除、拖动排序和空/加载/失败状态均已验证；完整 `npm run check`（含 35 个测试文件、147 个用例）通过。
- [x] `P0-WORKSPACE-108C` 重做项目级外壳。进入项目后左侧项目列表默认隐藏，以内容边缘的方向箭头控制抽屉平滑滑入/滑出；抽屉宽度保留最小/最大约束，展开后可切换项目，关闭后不占主内容布局宽度。右侧只保留一个项目级多轮对话实例，跨一级/二级页面切换不卸载、不清空、不创建重复会话，并继续支持可拖动宽度和移动端呈现。右上角删除重复的语言/主题控件；数据刷新动作保留时使用明确工具提示、加载状态和可访问名称说明“重新从服务端获取当前项目数据”。项目抽屉、常驻对话和刷新控件使用 Apple 风格磨砂玻璃、半透明背景、`backdrop-filter` 模糊、柔和阴影与弹簧式滑入滑出，拖动宽度实时响应且无突兀跳变。 [Apple 设计验收]

> 验收记录（2026-08-04）：项目抽屉默认收起且不占主内容宽度，真实浏览器点击边缘箭头后平滑滑入；桌面 1440 与移动 390 均无横向溢出，移动端带毛玻璃遮罩；键盘调整抽屉宽度从 220px 实时更新到 230px；项目对话在切换 `项目总览 -> Idea 讨论` 后保持同一实例和草稿；顶栏已移除重复语言/主题菜单，刷新按钮带加载态与“重新从服务端获取当前项目数据”可访问名称。抽屉触发控件视觉上只保留 18px 宽透明命中区内的纯方向窄三角（关闭时 `▷`、展开时 `◁`），无按钮文字、无边框、无背景块、无胶囊圆角；悬停时三角变蓝并沿指向方向轻微前移，展开/收起时以弹簧曲线旋转 180°，并保留可访问名称、`aria-expanded` 与键盘焦点。
- [x] `P0-WORKSPACE-108D` 按新合同重做项目概述，二级顺序固定为 `项目总览 -> Idea 讨论 -> 待审批与决策 -> 定期汇报与反馈`。总览聚合研究目标、描述、创新候选、状态、项目进度、实验/审批/论文摘要和最近事件；Idea 页合并旧规格、Idea 对话、创新与边界和研究现状矩阵中与 novelty 有关的内容，并支持对矩阵做可审阅的排列组合探索以生成未覆盖创新候选；审批页汇总全项目 Proposal 和修订意见；汇报页只从真实工作流事件生成报告与反馈。迁移后不得留下内容相同的重复页，前端不得出现“导师/老师”等角色文案；总览、审批和汇报使用 Apple 风格玻璃分层卡片、半透明分隔、柔和阴影，待办/状态变化有平滑动效。 [Apple 设计验收]

> 验收记录（2026-08-04）：项目概述二级顺序固定为 `项目总览 -> Idea 讨论 -> 待审批与决策 -> 定期汇报与反馈`。项目总览展示研究规格、项目描述、创新点候选、研究进度时间线和项目状态；Idea 页整合研究规格、风险/开放问题边界、常驻对话说明与创新矩阵探索，创新矩阵通过 `research-status/gap-candidates` 记录待核验候选；审批页汇总 Proposal、Idea 修订意见和项目策略；汇报页统一展示日报/周报、反馈收件箱、记录反馈和 Proposal 审计，报告无来源快照时如实显示阻塞状态。真实浏览器检查四个二级页均正常渲染，界面无“导师/老师”等角色文案；完整 typecheck、web build、UI/i18n、navigation、language-boundary 检查通过，research-status/reports/feedback/report-lineage 相关服务端测试 13 项通过。
- [x] `P0-WORKSPACE-108E` 按新合同重做相关工作调研，二级顺序固定为 `文献列表 -> 研究可视化 -> 种子文献扩展`。文献行完整呈现作者、机构、年份、引用量、来源、全文/下载、项目主页和明确代码仓库；可视化包含年份/时间线与可交互引用图，节点大小来自真实引用量；种子扩展支持从参考文献和后续引用双向检索，并把已审计代码候选交给相关工作实现。自动找论文部分可参考 `/mnt/d/auto-related-work` 的算法、字段 schema、缓存策略、测试思路和数据处理设计，但必须迁移为 TypeScript，不能直接运行或依赖 Python。所有未知、partial、失败、取消、去重和 provenance 状态必须可见；文献、图表和来源抽屉采用 Apple 风格毛玻璃容器、柔和阴影与细腻动效，图节点/时间线切换不跳变。 [Apple 设计验收]

> 验收记录（2026-08-04）：相关工作调研三个二级页已拆分：`文献列表` 只呈现确认论文、材料检索、论断复核与代码仓库候选；`种子文献扩展` 只呈现种子、递归参数/运行、provider 失败、引用边、候选与字段 provenance，不再与文献列表重复；`研究可视化` 增加按真实发表年份统计的柱状时间线，引用图节点宽度/高度按论文真实引用量缩放并保留键盘/鼠标交互与来源详情。文献行补齐作者、机构、年份、引用量、来源、全文/PDF、项目主页和代码仓库链接。真实浏览器检查三个页面均正常，图节点出现多种尺寸且年份柱状图按项目论文生成；Web 类型检查、构建、UI/i18n、navigation、language-boundary 与 research-status/related-work 服务端测试通过。
- [x] `P0-WORKSPACE-108F` 按新合同重做实验实现，只保留 `本方法实现 -> 相关工作实现` 两个二级标签并彻底删除第三级标签栏。两个页面共用“左侧实验列表 + 右侧详情工作区”：列表覆盖待做、等待审批、排队、运行、完成、失败、取消；详情覆盖目的/依据/目标/方法、文件树与代码查看、Git 状态与提交、实验计划、运行队列、seed、指标、Artifact 预览和谱系。本方法实验支持两条入口：用户通过项目对话直接提出实验，或 AI 分析后生成待审批 Proposal，用户可在项目对话中微调细节后审批；图片、曲线、CSV/JSON、PDF、点云和 3D mesh 只能渲染真实产物；原“变更与审批”入口迁移到项目概述。相关工作实现额外绑定 Paper、Evidence、仓库验证和固定 commit，无代码时通过项目对话讨论复现方案，本方法与复现源码/Git 永久隔离；实验列表、文件树、Git 状态和产物预览采用 Apple 风格半透明侧栏、玻璃工作区、柔和阴影和流畅的队列/状态动效。 [Apple 设计验收]
> 验收记录（2026-08-04）：`实验实现` 现只保留 `本方法实现` 与 `相关工作实现` 两个二级标签，页面内无第三级标签。两个页面共用 `ExperimentWorkspace` 双栏工作区：左侧是方法工作项/实验或代码仓库/复现/运行记录，右侧按选中项展示方法设计、实验状态、运行队列、指标、相关审批、受限文件树、Git 状态、真实 Artifact 预览和谱系。复现详情绑定论文、仓库验证、固定 commit 与源码路径，并提供下载/依赖/运行审批表单和复现边界说明；服务端工作区接口支持 `method`/`reproduction` 范围且只允许当前项目的复现记录。真实浏览器检查方法页 7 个工作项与 46 个产物、复现页 5 个仓库候选均正常，明暗主题字体/背景可读、无横向溢出、详情区可滚动；Web 类型检查、构建、UI/i18n、navigation、language-boundary、相关服务端测试与新增工作区范围回归测试通过。
- [x] `P0-WORKSPACE-108G` 按论文内容重做学术论文撰写，二级顺序固定为 `引言 -> 相关工作 -> 方法介绍 -> 实验 -> 结论`，删除以“论文项目/引用/BibTeX/图表/数据/编译/PDF”为顶层标签的旧工具式导航。每节编辑器显示英文稿件、逐句对应中文翻译、引用和上游来源；中文翻译只用于界面理解，不进入 PDF。论文使用独立项目内 Git、默认 CVPR 模板和 Linux `latexmk`，AI 生成/修订后自动编译并展示 PDF 或错误；常驻项目对话可指导章节修改、图表绘制与插入位置、流程图和整体可视化。任何生成内容必须受证据、实验与审批门禁约束；论文编辑、翻译面板和 PDF 审阅使用 Apple 风格毛玻璃工具条、半透明分栏、柔和阴影与平滑的章节切换动效。 [Apple 设计验收]

> 进度记录（2026-08-04）：旧工具式 `PaperTab` 已删除，五个二级标签改由新的 `PaperWorkspace` 承载。新增 `GET /api/projects/:id/paper-workspace` 解析 `paper/main.tex`、`references.bib` 与 `paper/translations.json`，按章节返回英文稿件、逐句中译、引用键、图表引用、Git 状态与编译摘要；`POST /api/projects/:id/paper-section` 把逐节修订生成为受审批门禁约束的 `code_patch` Proposal，批准后才写入稿件。前端提供章节列表、英文稿件/中文翻译双栏、引用与上游来源、图表引用、编译状态与 PDF 卡片，以及“编辑本节”LaTeX 编辑器；中文翻译明确标注仅界面参考。真实浏览器检查桌面 1440 下五个章节切换、双栏渲染、翻译展示、编辑模式开合、编译/PDF 卡片均正常，无横向溢出，详情区可滚动；Web 类型检查、构建、UI/i18n、navigation、language-boundary 与新增论文工作区服务端测试通过。已继续加入 `POST /api/projects/:id/paper-translate` 与 `POST /api/projects/:id/paper-revise`：通过 Mastra 新增 `paper-translate`、`paper-revise` 两个结构化 Agent，前端提供“AI 中译”和“AI 修订”按钮，翻译仅用于界面参考，AI 修订生成受审批门禁的 `code_patch` Proposal；模型失败按规范结构化报错，无静默兜底。2026-08-04 已补齐剩余代码链路：默认草稿改为自包含 CVPR 风格模板（`paper/cvpr.sty` 与双栏 `article` 前导一起进入受审批 Patch），批准 `patch_kind=latex` 的 Proposal 后自动排队 `compile_latex`，实测 demo 项目批准草稿后 latexmk 成功产出真实 PDF Artifact；`ensureProjectGit` 为缺失 `.git` 的旧项目自动初始化项目内 Git 并写入项目级 `.gitignore`，论文工作区返回真实 `source_commit`/`source_dirty`；`paper-translate` 成功结果原子写回 `paper/translations.json` 并记录审计，新增持久化测试。前端 `PaperWorkspace` 在编译排队/运行中每 4 秒自动刷新状态与 PDF。

> 阻塞记录（2026-08-04）：默认文档模型 `deepseek-v4-flash` 的上游 403 `RegionError` 已通过运行时模型设置配置 `grok-4.5` 解除；真实 `POST /api/projects/:id/paper-translate` 与 `paper-revise` 均成功，翻译写回 `paper/translations.json`，修订生成受审批门禁的 `code_patch` Proposal。默认模型仍保持 `deepseek-v4-flash`，未配置可用模型时继续结构化失败，不静默降级。
- [x] `P0-WORKSPACE-108H` 完成全链路和视觉验收。同步严格 Zod API、JSON Schema、数据库迁移、i18n 四语言、浅/暗主题、README、AGENTS、架构/运维/安全文档和 `DOCS_SYNC_VERSION`；在 Windows Chrome 验收桌面/窄桌面/移动端的首页、抽屉、常驻对话、全部新标签、长内容滚动、拖动宽度、动画、键盘/读屏、空/加载/partial/失败/审批状态。运行完整类型检查、测试、构建、导航/UI/i18n、主链、Mastra HITL、实验与 LaTeX 适用验收；外部服务失败保持结构化阻塞；视觉验收必须逐项确认 Apple 设计契约：磨砂玻璃、半透明、`backdrop-filter`、柔和阴影、动效曲线、明暗主题、窄屏与移动端，并检查 `prefers-reduced-motion`。 [Apple 设计验收]

> 进度记录（2026-08-04）：文档同步已完成（README、README.zh-CN、AGENTS、架构、运维 + `DOCS_SYNC_VERSION=2026-08-04-02`）。新增 `scripts/browser-acceptance.mjs` 完成真实 Headless Chrome 验收并输出 `runtime/preview/108h-*` 截图：桌面 1440 的首页/项目页在 `zh-CN`、`zh-TW`、`en`、`es` 四种语言下均无横向溢出且 `lang`/主题正确；暗色主题首页/项目页无横向溢出；移动 390 首页/项目页无横向溢出，并修复了移动端侧栏拖拽条外溢 5px 的问题；抽屉触发控件保留纯窄三角符号，键盘聚焦后 `aria-label`、`aria-expanded`、`aria-controls` 与面板均有效；`prefers-reduced-motion: reduce` 下抽屉过渡为 1e-05s 且能直接展开到 `translateX(0)`。扩展验收覆盖 404 四语言/暗色/移动、设置面板四语言/暗色/移动、删除确认浅暗、品牌首页与悬停、主题刷新持久化、长内容滚动与项目操作动效，均通过。Mastra HITL 验收通过（suspend/resume/reject + 审计绑定）；原生实验运行时验收通过（项目内 `.venv`、真实指标、Artifact SHA-256、进程树取消）；完整服务端测试 38 个文件、158 个用例全部通过；web/server typecheck、web build、ui/i18n、navigation、language-boundary、docs 检查通过。默认 CVPR 论文模板升级后，`claim-review-api` 中检查旧纯文本标题的断言已更新为检查 `\section{Related Work}` 与真实 claim 文本，回归测试通过。

> 阻塞记录（2026-08-04）：已解除。修复了 Mastra 读取 `RESEARCH_RUNTIME_DIR/model-settings.json` 的路径不一致问题，界面保存的模型配置现在对 Mastra 生效；运行时文档模型配置为网关可用的 `grok-4.5` 后，`/api/chat` 端到端真实回复成功。另修复 Supermemory 输入处理器把整份项目 JSON 当作 12k 字符查询、导致本地嵌入超时的问题：现在只提取真实用户消息（最多 2000 字符）进行语义检索。`npm run acceptance` 通过（2 次真实模型调用），完整 typecheck、158 个服务端测试、build、docs/navigation/UI/i18n/language-boundary、Mastra HITL 与浏览器验收均通过；未配置可用文档模型时保持结构化失败，不静默降级。
- [x] `P0-UI-109` 项目抽屉与项目列表交互润色：抽屉触发控件改为与侧栏拖动条一致的蓝色细线样式；移除侧栏中与首页品牌重复的“新研究项目”按钮；首页置顶状态只由操作列按钮呈现（置顶=蓝色高亮，取消置顶=普通颜色），标题旁不再出现第二个置顶图标；置顶与拖拽排序改为本地平滑更新，成功响应后直接更新列表，不再触发整页加载刷新；抽屉打开时点击右侧工作区会自动收起，无需再点收起按钮。 [Apple 设计验收]

> 验收记录（2026-08-04）：真实浏览器验收确认侧栏不再存在 `.primary.full` 新项目按钮，首页标题旁 `home-pinned-icon` 数量为 0，操作列置顶按钮的 `is-pinned` 与 `aria-pressed` 随置顶状态一致；抽屉触发控件 `::before` 为 `rgba(0,122,255,.18)` 的 2px 蓝色细线，悬停/聚焦加深为系统蓝；点击右侧 `.tab-content` 后抽屉自动收起。置顶与排序接口成功后只更新本地 `projects` 状态，不再调用 `loadProjects()`，不会出现首页加载骨架或整页刷新。Web 类型检查、构建和完整浏览器验收（四语言、明暗主题、桌面/窄桌面/移动、404、设置、删除、长内容、动作层、标签、侧栏拖动）均通过。
- [x] `P0-UI-110` 置顶与项目排序全程无刷新、行移动动效丝滑：置顶/排序点击后先立即更新本地列表，成功时按服务端返回校准，失败时静默回滚到点击前状态并只提示错误，不再触发首页加载骨架或整页刷新；首页项目行与侧栏项目行在置顶、取消置顶、拖拽排序后使用平滑的行移动动画，动画可中断并兼容 `prefers-reduced-motion`。 [Apple 设计验收]

> 验收记录（2026-08-04）：`pinProject` 与 `reorderProjects` 均先按本地预览更新 `projects`，成功时用服务端返回值校准，失败只回滚快照并提示错误，不再调用 `loadProjects()`。首页与侧栏项目列表增加 FLIP 行移动动画：真实浏览器在 `prefers-reduced-motion: no-preference` 下点击未置顶项目，立即出现 `translateY` 中间帧并在约 300ms 内收敛到目标位置；整个过程中无 `.home-loading` 骨架、路径保持 `/`、`navigation` 事件数仍为 1，恢复置顶后行数与顺序不变。`prefers-reduced-motion: reduce` 下跳过行移动动画。Web 类型检查、Web 构建、UI/i18n、navigation、language-boundary 和完整浏览器验收均通过。
- [x] `P0-UI-111` 置顶标识去重与抽屉触发控件风格统一：首页和侧栏的置顶状态都只由操作列按钮呈现（置顶=蓝色高亮，取消置顶=普通颜色），行标题旁不再出现任何置顶图标；抽屉触发控件保持窄三角方向提示，同时整体视觉采用与侧栏拖动条一致的蓝色细线、圆角光晕和系统蓝悬停效果，符合 Apple 设计契约。 [Apple 设计验收]

> 验收记录（2026-08-04）：侧栏行标题内的 `project-pinned-indicator` 已移除，首页与侧栏均不再出现标题旁置顶图标；抽屉触发控件 `::before` 为 3px 蓝色细线（默认 `rgba(0,122,255,.26)`），悬停/聚焦变为系统蓝并带 6px 柔和光晕，窄三角保留方向提示。完整浏览器验收通过：置顶点击全程无 `.home-loading`、无导航刷新、行移动有中间帧且收敛丝滑，抽屉打开后点击右侧工作区自动收起，侧栏无“新研究项目”按钮。

- [x] `P0-UI-112` 按用户最新反馈继续润色可见交互：项目抽屉收起/展开按钮采用与拖动宽度一致的蓝色胶囊手柄样式（保留窄三角方向提示，浅/暗主题都使用语义变量和磨砂玻璃）；首页项目拖拽从原生 HTML5 DnD 改为与侧栏一致的指针式长按拖拽，拖动预览与放下都使用 FLIP 平滑行动画，不再出现原生拖影/整页式刷新感；置顶与排序成功后只做一次本地收敛，不重复触发列表重排。抽屉打开后点击右侧工作区自动收起继续可用。 [Apple 设计验收]

> 验收记录（2026-08-04）：真实浏览器 CDP 验收确认首页行不再带 `draggable`（0 个），操作列仍只有 1 个置顶按钮且标题旁无置顶图标；置顶点击后路径保持 `/`、无 `.home-loading` 骨架、`aria-pressed` 正确更新；指针式长按拖拽排序后路径保持 `/`、无加载骨架且顺序实际变化；抽屉触发控件为 24×58 的 999px 圆角蓝色胶囊，半透明背景 + `backdrop-filter`，浅/暗主题使用语义变量；抽屉展开后点击右侧工作区自动收起（CDP 真实 pointerdown 验证 closed=true）。完整 `scripts/browser-acceptance.mjs`、`npm run ui:check`、`npm run language-boundary:check`、`npm run navigation:check`、Web 类型检查与 Web 构建均通过。

- [x] `P0-UI-113` 按用户再次反馈收敛左侧抽屉开关视觉：收起/展开按钮从 24×58 蓝色胶囊改为更贴近侧栏拖动条的窄蓝线手柄（保留窄三角方向提示，浅/暗主题使用语义变量）；彻底清理已移除的“新研究项目”按钮翻译残留；置顶、拖拽排序保持无整页刷新，抽屉打开后点击右侧工作区自动收起继续可用。 [Apple 设计验收]

> 验收记录（2026-08-04）：真实浏览器 CDP 确认抽屉触发控件为 16×84 透明命中区，仅保留 2px 蓝色细线与窄三角，无边框/背景/大胶囊；首页 `.primary.full` 新项目按钮数量为 0，页面文本不再包含“新研究项目”残留；置顶点击后路径保持 `/`、导航计数不变、无 `.home-loading` 骨架；抽屉展开后点击右侧工作区自动收起（pointerdown 验证 closed=true）。`npm run typecheck`、`npm run build --workspace @research-os/web`、`npm run ui:check`、`npm run language-boundary:check`、`npm run navigation:check` 均通过（tsx 脚本需以 `TMPDIR=/tmp` 规避 `/mnt/c` 管道监听限制）。

- [x] `P0-UI-114` 按用户最新反馈把抽屉触发控件做成与侧栏拖动条一致的蓝色手柄样式：22×96 圆角胶囊、半透明磨砂背景、2px 系统蓝竖线与窄三角方向提示，浅/暗主题都使用语义变量；确认侧栏不存在独立的“新研究项目”按钮，首页标题旁没有第二个置顶图标，操作列只保留一个置顶按钮；置顶与指针式拖拽排序只更新本地 `projects`，不触发整页刷新或加载骨架；抽屉打开后点击右侧工作区或聚焦右侧内容自动收起。 [Apple 设计验收]

> 验收记录（2026-08-04）：真实浏览器 CDP 确认抽屉触发控件计算样式为 `22px × 96px`、`border-radius:999px`、浅色线性渐变磨砂背景、`rgba(0,122,255,.14)` 边框与 2px 蓝色 `::before` 竖线，窄三角保留；首页 `.sidebar .primary` 新项目按钮数量为 0，页面文本不含“新研究项目”，`home-project-title-line` 内无置顶按钮，`home-project-actions .home-pin-action` 每行恰好 1 个；置顶点击后路径保持 `/`、无 `.home-loading` 骨架、fetch 仅 `/api/projects/:id/pin`；指针式长按拖拽后路径保持 `/`、无加载骨架、fetch 仅 `/api/projects/order`；抽屉展开后 dispatch 到右侧工作区的真实 `pointerdown` 使 `open=false`。`npm run typecheck --workspace @research-os/web` 与 `npm run build --workspace @research-os/web` 均通过。

- [x] `P0-UI-115` 按用户最终反馈彻底移除抽屉触发控件上的三角形，只保留 2px 系统蓝细竖条：收起状态短竖条水平中心位于 `x=0`，同时隐藏用于调节侧栏宽度的长蓝色竖条；展开状态才显示长 resize 竖条，短竖条随抽屉右缘滑动；悬停/聚焦时蓝线变纯色并加柔和光晕。 [Apple 设计验收]

> 验收记录（2026-08-04）：真实浏览器 CDP 计算样式确认触发控件为 `20×84`、`background:none`、`border-radius:0`、`::before` 2px 系统蓝线；视觉模型（`mimo-v2.5`）分别检查浅色关闭、浅色打开、暗色关闭、暗色打开四张截图，均描述为“细蓝线 + 小三角、无胶囊背景”，打开状态三角指向左、关闭状态指向右；抽屉展开 `translateX(0)` 后把手位于侧栏右缘。`index.html` 静态资源版本号更新为 `20260804-ui125`；`npm run typecheck --workspace @research-os/web`、`npm run build --workspace @research-os/web` 与 `npm run ui:check` 均通过。

> 验收记录（2026-08-04）：`DrawerArrow` 组件与全部 `.project-drawer-arrow` CSS 已删除，触发按钮不再渲染三角形；收起状态下 CDP 实测 `.project-drawer-toggle` 为 `left:-10px`、`width:20px`、`::before left:9px`，短蓝条中心 `lineCenter=0`，`.sidebar-resizer` 为 `opacity:0` 且 `pointer-events:none`；展开状态下短蓝条位于侧栏右缘（`lineCenter=220`），resize 长条恢复 `opacity:1` 与 `pointer-events:auto`。`index.html` 静态资源版本号更新为 `20260804-ui127`；`npm run typecheck --workspace @research-os/web` 与 `npm run build --workspace @research-os/web` 均通过。

> 验收记录（2026-08-04）：用户反馈短蓝线与长蓝线视觉粗细不一致；两者 CSS 宽度本就同为 `2px`，差异来自短线的半透明蓝与浅色光晕。已将短蓝线默认颜色与光晕统一为与长线悬停/拖动状态一致的实心系统蓝 + `0 0 0 4px var(--blue-soft)`，hover/focus 保持同参数。`index.html` 静态资源版本号更新为 `20260804-ui128`。

> 验收记录（2026-08-04）：按用户进一步要求，短蓝线改为与长 resize 竖线完全一致的样式状态：默认透明，hover/focus 才变为实心系统蓝 + `0 0 0 4px var(--blue-soft)`；收起时短线左边缘位于 `x=0`（`.project-drawer-toggle { left:-9px; }`、`::before { left:9px; }`），展开时短线水平中心对齐侧栏右边界（toggle 位于 `sidebarWidth - 10px`）；抽屉外壳与把手统一使用 `0.5s var(--spring)` 水平滑动。`index.html` 静态资源版本号更新为 `20260804-ui129`。

> 验收记录（2026-08-04）：浏览器 CSSOM 实测发现暗色主题残留 `:root[data-theme='dark'] .project-drawer-toggle::before` 半透明蓝 + 5px 光晕及 hover 6px 光晕覆盖规则，导致与长线不一致；已删除这些暗色专属覆盖，短蓝线在浅/暗主题均与长线共用默认透明、hover/focus 实心蓝 + `0 0 0 4px var(--blue-soft)`。CDP 实测收起 `lineLeft=0`、展开 `lineCenter=sidebarRight=220`，resize 长条收起隐藏、展开显示，两侧 `transition` 均为 `0.5s` 弹簧曲线。

- [x] `P0-RELATED-115` 按 `/mnt/d/auto-related-work` 的算法尽可能完整补齐 TypeScript 相关工作引擎：完整 BibTeX 解析（作者列表、venue、年份、DOI、摘要）并写入用户输入 provenance；Unpaywall 按 DOI 补全开放获取 PDF；补全引擎改为多轮迭代，按 Python 版完整性阈值（85%）提前停止，并把 Crossref/OpenAlex/DBLP/arXiv 的作者机构按姓名增量合并；arXiv API 按 ID 拉取完整作者与摘要，arXiv HTML5 作者机构、邮箱与通讯作者解析作为显式可审计的 arXiv 补全策略；Google Scholar 爬虫、Cookie、住宅代理和 CAPTCHA 隧道仍按 AGENTS 绝对禁止，不迁移。完成前不把测试 fixture 或旧缓存冒充真实来源。 [Apple 设计验收]

> 验收记录（2026-08-04）：与 `/mnt/d/auto-related-work` 的 `references.py`、`recursive_search.py`、`enrich_fields.py`、`scholar_search.py` 逐项对照后补齐：嵌套花括号 BibTeX 解析、Python 版完整度评分与 85% 提前停止、多轮字段补全、Unpaywall OA PDF、按姓名增量合并机构、arXiv API 完整作者/摘要、arXiv HTML5 机构/邮箱/通讯作者，并修复同一 provider 多次补全时内存目标未同步导致完整作者列表被覆盖的问题；Google Scholar 爬虫、Cookie、住宅代理和 CAPTCHA 隧道仍不迁移。新增 Unpaywall/arXiv HTML/arXiv ID/作者替换与多轮 API 测试；真实外部抽查 `10.1371/journal.pcbi.1004668` 拿到 OA PDF、`1706.03762v1` 解析 8 位作者与 Google Brain 机构。`.env.example` 补充 `UNPAYWALL_EMAIL`/`RESEARCH_CONTACT_EMAIL`，README/README.zh-CN 与 `DOCS_SYNC_VERSION=2026-08-04-03` 同步。`npm run typecheck`、完整服务端 171 项测试、`npm run build`、`docs:check`、`ui:check`、`language-boundary:check`、`navigation:check` 与 `related-work:restart:check` 全部通过。

### 4.1 旧任务池（当前非优先）

> 以下 `4.1.1` 至 `4.1.12` 是之前的 TODO 收集区。它们仍被保留，但明确不是当前优先；只有 `P0-WORKSPACE-108A` 至 `108H` 完成并通过验收后，才重新评估。已被新工作台取代的旧结构（第三级实验标签、论文工具式顶层标签、旧首页 Idea 流程等）不得借旧任务恢复；已完成项 `[x]` 只作为历史记录。

> 冲突清理记录（2026-08-03）：`070-A2`、`078o`、`078j`、`078l` 等要求旧三行导航/论文工具式页面的条目已删除，不得恢复；旧任务池中任何与 `108A-H` 冲突的表述一律以 `108A-H` 为准。本文下面把残留的“日报/周报”“六个工作包”“Idea 聊天”等旧表述改写为新合同用语。

#### 4.1.1 整体界面、导航与页面状态（旧）

- [x] `070-A0` 检查 favicon 和左上角品牌小图标在 16/27/30px、高 DPI、亮色/暗色背景下的显示效果、加载路径和无障碍名称。 [Apple 设计验收]

> 验收记录（2026-08-04）：`index.html` 的 `link[rel=icon]` 指向 `/favicon.svg`，SVG 可加载且含 `<title>Research OS</title>` 与无障碍描述；品牌按钮 `aria-label="Go to home"` 可点击回首页，品牌图标在紧凑桌面为 25px、标准/窄屏规则提供 27/30px 尺寸，真实浏览器浅色/暗色主题均显示且无横向溢出；`deviceScaleFactor: 2` 下捕获 2880×1800 高 DPI 截图，图标正常加载。已纳入 `scripts/browser-acceptance.mjs` 常驻验收。
- [x] `070-A3` 给每个二级页面补齐完整状态展示：空、加载、成功、失败、等待审批、已取消、部分成功、无证据、无权限；每种状态都要显示错误码、来源、项目范围、能否重试和下一步建议。 [Apple 设计验收]

> 验收记录（2026-08-04）：新增 `StateNotice` 通用状态卡（loading/ready/empty/error/partial/waiting/cancelled/no_evidence/no_permission），统一展示错误码、来源、项目范围、可重试状态、下一步与重试按钮；项目工作区顶栏新增“状态与下一步”面板，9 类状态图例逐项说明下一步，当前状态根据项目最新失败/待审批/刷新状态实时呈现。文献材料搜索、研究可视化图/代码工作区、实验文件树、论文工作区等页面的加载/失败状态已切换为 `StateNotice`，四语言 i18n 全部补齐；真实浏览器 CDP 验收确认状态面板存在、9 个状态 chip、9 行图例含元数据、项目范围可见、可重试标记正确、点击展开正常且无横向溢出。同时把 `migrate()` 改为逐条执行迁移 SQL，避免 PGlite 在已有数据库上多语句 `exec` 触发 WASM 崩溃；完整服务端 38 个文件、158 个用例、Web 类型检查、Web 构建、UI/i18n、navigation、language-boundary 与浏览器验收均通过。
- [x] `070-A4` 在 Windows Chrome 里对桌面、窄桌面、手机宽度各做完整截图，确认项目列表、二级标签、主内容区、表格、Markdown、diff、图表不溢出、不重叠；滚动条保留可用性但不显示突兀的原生样式。 [Apple 设计验收]

> 验收记录（2026-08-04）：`scripts/browser-acceptance.mjs` 已遍历项目工作区全部 14 个二级页（项目总览/Idea/审批/汇报、文献/可视化/种子扩展、本方法/相关工作实现、论文五节），在桌面 1440 与移动 390 分别捕获截图并检查 `scrollWidth === innerWidth`，全部无横向溢出；原有 1024 窄桌面首页/项目页、长内容滚动与配置/404/删除弹窗截图继续纳入常驻验收。滚动条统一使用细滚动条规则，长内容区 `overflow-y:auto` + `scrollbar-width:thin`。
- [x] `070-A5` 每个页面顶部统一显示项目范围、更新时间、权限、最近失败和待审批动作；切换项目后不能残留上一个项目的候选、图表、报告、反馈或记忆。 [Apple 设计验收]

> 验收记录（2026-08-04）：项目工作区顶部统一渲染 `.workspace-context`，固定显示当前项目 UUID/范围、更新时间、项目范围内权限、Idea 版本、待审批数量和最近失败（无失败时显示“最近失败：无”）；真实浏览器从 `uncertainty-based-d9a5` 切换到另一项目后，范围 ID 与顶栏标题均变化，待审批/失败状态随项目数据更新，无上一个项目的上下文残留，并捕获 `108h-context-switch.png`。已在 `scripts/browser-acceptance.mjs` 纳入常驻验收。
- [x] `085d` 统一桌面端的紧凑间距、标题、按钮和面板尺寸，减少内容被截断的情况；完成桌面/窄屏/移动端真实浏览器验收后再关闭。 [Apple 设计验收]

> 验收记录（2026-08-04）：`scripts/browser-acceptance.mjs` 已在 1440 桌面、1024 窄桌面和 390 移动宽度分别打开首页与项目页，`scrollWidth === innerWidth`，无横向溢出；紧凑样式由 `apple-glass.css` 的响应式密度规则统一驱动，按钮、面板和标题尺寸均通过真实浏览器截图确认。

#### 4.1.2 多语言、主题与 404 页面（旧）

- [x] `P0-SETTINGS-112` 左下角模型设置新增“图片识别 / 图片生成”两个用途页签，并为每个已配置模型（代码三档、文档文本、图片识别、图片生成、语音识别）提供“测试连接”按钮。图片识别默认 `mimo-v2.5` + `http://10.31.107.77:3000/v1`，聊天/Idea 讨论带图片附件时优先使用该模型；图片生成默认 `gpt-image-2-official` + `https://api.apimart.ai/v1`，支持 1k/2k/4k 与 low/medium/high，默认 1k/low/n=1。测试按钮只发送最小请求验证连通性，图片生成测试提交一个最省钱的真实任务但不下载不展示。通用页的语言与主题改为草稿状态，点击“保存配置”后才生效。密钥只写入 `runtime/model-settings.json`，读取接口只返回 `key_configured`。 [Apple 设计验收]

> 验收记录（2026-08-04）：真实浏览器打开设置面板，六个模型二级页签（代码模型、文档文本、图片识别、图片生成、Embedding、语音识别）均存在且无横向溢出；图片识别/图片生成页签渲染对应标题、字段与“测试连接”按钮，图片生成分辨率/质量下拉正常，桌面 1440 无溢出。通用页语言/主题改为保存后生效：真实浏览器点击暗色后主题仍为浅色，点击“保存配置”后变为暗色。服务端 `/api/settings/vision`、`/api/settings/image-generation`、`/api/settings/model-test` 已验证；`model-test` 对本地文档模型真实返回成功，图片生成/视觉目标在当前开发网络不可达时返回结构化错误而非静默成功。服务端 173 项测试、Web/server/Mastra 类型检查与构建、UI/i18n、docs、navigation、language-boundary 均通过；README/README.zh-CN/AGENTS/.env.example 已同步。
- [x] `P0-SETTINGS-106` 重新设计左下角配置面板为“通用 / 模型”两级入口：模型页内使用第二级标签栏（代码模型、文档文本、Embedding、语音识别）；代码模型只使用“轻量级/通用/最强大”三个通用档位，不再显示 Luna/Terra/Sol 品牌名；新增文档文本模型（默认 `deepseek-v4-flash`、`http://127.0.0.1:3000/v1`），并接入聊天中的解释性回复链路；全局代理关闭时不显示 URL 输入框，开启时与开关同一行展示输入框。界面、API 与代理交互已完成浏览器与测试验收；默认文档模型在本机网关的 403 可用性问题单独登记为 `DOC-MODEL-107`。 [Apple 设计验收]
- [x] `P0-SETTINGS-105` 重新设计左下角配置面板：按“通用 / 模型 / 语音 / Embedding”分组，把主题、语言、全局代理归入通用设置；语音识别支持任意 OpenAI 兼容接口 URL，不再只面向 Groq；修复 Groq/兼容 API 录音转写返回空文本的问题。已完成 Web 类型检查与构建、145 项服务端测试、docs 与语言边界检查，并在 Windows Chrome 中验证四栏布局、通用语言/主题/代理、语音 API 模式与代理从模型页移除。 [Apple 设计验收]
- [x] `P0-VOICE-KEY-104` Groq 语音识别支持在界面配置 API key：默认读取 `.env` 的 `GROQ_API_KEY`，留空保留现有 key，读取接口只返回 `key_configured`，运行时 key 以 0600 权限写入 `runtime/voice-settings.json`。已通过 Web 类型检查与构建、UI/i18n、docs 检查和完整服务端测试（142 项）。 [Apple 设计验收]
- [x] `P0-MODEL-PROXY-103` 模型配置增加全局代理开关（`enabled` + `url`），运行时控制 Mastra、模型桥接与 Groq 转写出口，关闭则直连、回环/私网目标始终直连；浏览器语音识别不再显示 Groq 模型/URL 配置。已通过代理直连/HTTP/HTTPS CONNECT 实测、Web 类型检查与构建、UI/i18n、docs 检查和完整服务端测试（141 项）。 [Apple 设计验收]
- [x] `P0-VOICE-PUNCT-102` 中文语音转写结果统一转换为全角中文标点（逗号、句号、问号、感叹号、分号、冒号、括号、引号等），Groq 与浏览器两条路径都应用；英文、西班牙语保持原标点，不破坏 URL、小数和英文缩写内部符号。已通过标点替换用例、Web 类型检查、Web 构建和 UI/i18n 检查。 [Apple 设计验收]
- [x] `079h` 设置面板内的语言和主题控件在窄屏不溢出；切换语言后文字变长时，按钮、标签、表格不破裂、不重叠。项目页右上角不得重复显示这两个控件。 [Apple 设计验收]

> 验收记录（2026-08-04）：`scripts/browser-acceptance.mjs` 在桌面 1440 与移动 390 打开配置面板，四种语言均正确渲染 `role="dialog"` 且无横向溢出；通用/模型两级入口正常，项目页右上角无重复语言/主题控件。
- [x] `079i` 四语言（简体/繁体中文、英语、西班牙语）桌面/窄屏截图验收：无漏译、无生硬译名、无重叠溢出。 [Apple 设计验收]

> 验收记录（2026-08-04）：桌面 1440 首页/项目页在 `zh-CN`、`zh-TW`、`en`、`es` 下均无横向溢出，`lang` 正确；配置面板四语言无溢出；移动 390 首页/项目页/配置面板无溢出。
- [x] `080h` 切换主题不改变语言和当前页面；刷新后恢复所选主题且无闪烁、无布局跳动。 [Apple 设计验收]

> 验收记录（2026-08-04）：真实浏览器把主题写入 `localStorage` 后刷新项目页，路径保持不变、`lang` 保持 `en`、`data-theme="dark"` 正确恢复，页面无横向溢出；`scripts/browser-acceptance.mjs` 的 `themePersist` 已纳入常驻验收。
- [x] `080i` 浅色/暗色两套主题的桌面/窄屏截图、颜色变量引用检查（禁止组件写死颜色）、持久化和无闪烁测试。 [Apple 设计验收]

> 验收记录（2026-08-04）：真实浏览器捕获暗色首页/项目页/配置面板/404，均无横向溢出且 `data-theme="dark"`；浅色四语言截图同批生成；主题仍由语义 CSS 变量驱动。
- [x] `081e` 404 页面完成四语言、浅色/暗色、桌面/窄屏的真实浏览器截图验收；当前代码已通过类型、构建、导航、UI/i18n 和文档/语言边界检查。 [Apple 设计验收]

> 验收记录（2026-08-04）：真实浏览器访问不存在的路径，`zh-CN`/`zh-TW`/`en`/`es` 均显示 404 卡片、路径与倒计时且无溢出；暗色与移动 390 同样通过。

#### 4.1.3 项目列表与工作区交互（旧）

- [x] `086d` 置顶/删除动作层、项目排序和多语言的真实浏览器验收；完整 server 测试、构建、导航/文档/语言边界检查已通过，浏览器截图和主链 acceptance 均已完成。 [Apple 设计验收]

> 验收记录（2026-08-04）：`scripts/browser-acceptance.mjs` 已把动作层滑入/收回、置顶/排序本地无刷新更新、删除确认磨砂遮罩、四语言与明暗主题纳入常驻验收：鼠标悬浮与键盘焦点动作层 transform 在 0ms/80ms/220ms/520ms 连续变化，置顶点击无 `.home-loading` 骨架且路径保持 `/`，删除弹窗 `z-index:100` 完整覆盖顶栏；桌面 1440/窄桌面 1024/移动 390 均无横向溢出。`npm run acceptance` 于 2026-08-04 通过 2 次真实模型调用、模型 key 隐藏、模型 URL 与 `.env` 一致和项目状态门禁，结果写入 `artifacts/acceptance/acceptance-20260804003245.json`。
- [x] `087c` 左上角 Research OS 品牌首页按钮的真实浏览器验收（图标与名称整体居中、可点击回首页）。 [Apple 设计验收]

> 验收记录（2026-08-04）：真实浏览器打开项目抽屉后点击品牌整体，路径回到 `/`；按钮存在可访问名称且图标与名称整体居中。
- [x] `088b` 项目操作按钮从右侧连续滑入、离开平滑收回动画的真实浏览器验收。 [Apple 设计验收]

> 验收记录（2026-08-04）：真实浏览器在项目行上派发鼠标悬浮，`.project-actions-track` 在 0ms/80ms/220ms/520ms 的 `transform` 分别约为 `66px → 51.4px → 7.6px → 0px`，离开后回到 `66px`；滑入与收回都使用同一 `transform` 过渡时间线，无跳变。
- [x] `089b` 品牌按钮悬停背景和垂直尺寸（完整包住图标、无突兀圆角）的真实浏览器验收。 [Apple 设计验收]

> 验收记录（2026-08-04）：品牌按钮高度完整包住品牌图标，`border-radius: 0px`；真实鼠标悬停后背景变为 `rgba(60, 60, 67, .06)` 的柔和透明高亮，无突兀圆角。
- [x] `090b` 置顶/删除图标悬停时单条连续时间线的真实浏览器验收。 [Apple 设计验收]

> 验收记录（2026-08-04）：置顶与删除图标各自只通过 `color/background/border-color/box-shadow/transform` 过渡，和项目行的 `padding-right`、动作层的 `transform` 使用同一组 Apple 弹簧缓动；真实浏览器采样无中间跳变。
- [x] `091b` 鼠标焦点与键盘焦点区分（只有键盘 `:focus-visible` 立即展开操作层）的真实浏览器验收。 [Apple 设计验收]

> 验收记录（2026-08-04）：真实浏览器验证程序聚焦后动作层保持在 `translateX(66px)`，只有 Tab 键盘聚焦会展开动作层；按 Tab 进入项目行后约 90ms 开始滑入，约 280ms 后稳定到 `translateX(0)`，主按钮同步让出 `padding-right: 66px`。
- [x] `092b` 逐帧 DOM 滑入动画的真实浏览器验收。 [Apple 设计验收]

> 验收记录（2026-08-04）：`scripts/browser-acceptance.mjs` 在 `actionMotion.samples` 中按真实时间采样动作层 transform，0ms 初始、80ms/220ms 中间帧、520ms 终点均连续变化，已纳入常驻验收。
- [x] `093b` 纯 CSS、立即开始、持续 0.5 秒滑入动画的真实浏览器验收。 [Apple 设计验收]

> 验收记录（2026-08-04）：动作层完全由 `.project-row:hover .project-actions-track` 与 `:focus-visible` CSS 规则驱动，无 JavaScript 介入；鼠标移入后过渡立即开始，`transition-duration: .5s`，使用 `cubic-bezier(.25, .1, .25, 1)`，并在离开后平滑收回。
- [x] `083b` 正文长内容页面的真实浏览器验收：滚动条可见、可操作，不产生页面级溢出，样式与项目列表滚动条一致。 [Apple 设计验收]

> 验收记录（2026-08-04）：论文编辑详情区 `paper-editor-pane` 真实出现 `scrollHeight > clientHeight`，`overflow-y: auto`、`scrollbar-width: thin`，页面级无横向溢出；滚动条样式与项目列表一致。
- [x] `084b` 删除垃圾桶图标动画、项目列表布局和确认框顶部磨砂遮罩的浅色/暗色截图验收。 [Apple 设计验收]

> 验收记录（2026-08-04）：真实浏览器浅色/暗色截图确认删除确认框可见，磨砂遮罩 `.modal` 的 `z-index: 100` 完整覆盖顶栏，`backdrop-filter: blur(18px) saturate(120%)` 生效；删除图标悬停变为红色软背景且带过渡动效，项目列表无布局挤压。
- [x] `082e` 项目删除/侧栏宽度/标签动画的真实浏览器截图验收。 [Apple 设计验收]

> 验收记录（2026-08-04）：浏览器验收环境已可用，解除旧沙箱阻塞。真实浏览器验证删除弹窗磨砂遮罩、键盘调整侧栏宽度（`220px → 230px`，恢复原值）、一级/二级标签的 `.sliding-tab-indicator` 在切换时平滑移动（`4px → 112px → 158px` 连续采样）；对应截图与指标已纳入 `scripts/browser-acceptance.mjs`。
- [!] `082a` 清理历史遗留的全局文件目录：旧 Supermemory 生成的部分文件属于其他用户（UID `10002`）且当前用户无删除权限，已复制到项目目录并审计为 `cleanup_pending`；解除条件是为该历史目录获得删除权限后再次运行迁移。 [Apple 设计验收]

#### 4.1.4 相关工作调研页面（旧）

- [x] `070-C1` 在网页上如实标明 DBLP/arXiv 只支持搜索、不支持引用递归；每次来源尝试的成功、无匹配、无效、限流、超时、取消都要可查。 [Apple 设计验收]

> 验收记录（2026-08-04）：`种子文献扩展` 页面新增“DBLP 与 arXiv 只支持关键词搜索，不支持引用递归；引用递归只使用 Crossref、OpenAlex 与 Semantic Scholar”能力说明，且在没有种子时也始终可见。新增“来源请求记录”面板：按项目展示每条真实来源尝试的 provider、状态、查询、请求地址、HTTP 状态、结果数、开始/结束时间和结构化失败，成功但零结果的请求显示“无匹配”，无记录时显示空状态。`related_work_source_attempts` 通过项目详情接口返回 `request_url`，服务端相关测试断言 seed 搜索后尝试记录含 `provider=crossref / status=succeeded / result_count=1 / request_url`；真实浏览器在 `related_work/seed-expansion` 验收能力说明、空状态与无横向溢出，Web 类型检查、构建、UI/i18n、navigation 与相关服务端测试均通过。
- [x] `070-C2` 补齐用户输入种子、Artifact 和候选来源的完整前端展示与校验，让用户清楚每条候选来自哪里。 [Apple 设计验收]

> 验收记录（2026-08-04）：`种子文献扩展` 的种子表单提供 DOI、标题、URL、BibTeX、受控 PDF 与已有论文六种输入，切换 PDF/已有论文时对应受控选项即时出现；种子列表展示输入摘要、来源类型、状态与时间，候选行展示 provider、发现深度、年份、DOI 和来源证据数，字段来源抽屉展示 provider、来源类型、请求尝试、Artifact、定位、哈希与原始值并支持逐字段选择。服务端严格 Zod 校验种子与项目范围，PDF 必须属于当前项目且为有效 Artifact，已有论文必须属于当前项目；用户 BibTeX 与字段选择来源测试覆盖 `user_input` provenance，跨项目种子/递归被拒绝。真实浏览器在 `related_work/seed-expansion` 验证六种选项与 PDF/已有论文联动且无横向溢出，Web 类型检查、构建、UI/i18n、navigation 与相关服务端测试均通过。
- [x] `070-C9e` 浏览器验收相关工作调研全流程：候选审阅、来源抽屉、去重理由、递归进度、研究现状矩阵、引用图、复现 Proposal，以及空/加载/部分/失败/阻塞状态和切换项目。 [Apple 设计验收]

> 验收记录（2026-08-04）：`scripts/seed-related-work-fixture.ts` 现在为 `uncertainty-based-d9a5` 写入 4 个递归运行、8 个来源 Attempt、3 条引用边与字段 provenance 冲突/选中样例；运行状态覆盖 `queued/cancelled/failed/completed`，Attempt 覆盖 `succeeded/partial/rate_limited/timed_out/invalid_response/unsupported/cancelled`，并保留真实失败消息（如 `Fixture provider timed out; no silent fallback was used.`、`DBLP does not support citation recursion`）。前端运行与 Attempt 失败提示直接展示结构化错误文本，不再被本地化成笼统的 “Request failed”。`scripts/browser-acceptance.mjs` 新增对应状态断言，并把材料搜索加载/失败状态移到 `related_work/literature` 页面验证；验收脚本在每次导航后启用 `Page.bringToFront` 与焦点模拟，避免后台标签页冻结动画。完整真实浏览器验收通过：候选审阅、来源抽屉、去重理由、递归进度、研究现状矩阵、引用图节点详情、复现下载 Proposal、材料加载/失败、跨项目空状态与切换项目均正常，桌面 1440/窄桌面 1024/移动 390 无横向溢出。

> 进度记录（2026-08-04）：本轮补齐相关工作验收数据与可见信息：候选行展示来源去重方式（DOI / provider stable ID / 标题+年份），引用网络运行展示逐条递归进度事件；新增相关工作验收 fixture，并在研究可视化页直接呈现研究现状矩阵；浏览器验收扩展候选审阅、来源抽屉、递归进度、图节点详情、复现 Proposal 与跨项目空状态检查，`scripts/browser-acceptance.mjs` 真实浏览器通过并输出 `108h-related-work-*` 截图。加载/部分/失败/阻塞状态仍需补齐为显式验收断言后才能关闭本项。
- [x] `070-C10d` 效果比较列表、详情、审批、指标/门禁/Artifact/候选状态的浏览器与窄屏截图验收。 [Apple 设计验收]

> 验收记录（2026-08-04）：`ComparisonTab` 已挂载到 `实验实现 -> 相关工作实现` 的右侧工作区，不再是无入口的死代码；表单、已保存比较卡片、指标表、比较候选与“保留/拒绝”审批按钮都真实渲染。新增毛玻璃半透明面板、柔和分层阴影、圆角候选卡、表格细滚动条和移动端单列折叠样式，明暗主题均使用语义变量。新增 `scripts/seed-comparison-fixture.ts` 写入一条“确认论文 + 已完成复现 + 已定位证据 + accepted ClaimReview + 真实比较与候选”的验收 fixture；完整浏览器验收在 1440 桌面验证 1 张比较卡、指标表、2 条候选、Keep/Reject 按钮且无横向溢出，点击 Keep 后出现 `Candidate retained` 状态，390 移动端同样无横向溢出并截图 `108h-comparison.png` / `108h-comparison-mobile.png`。Web/server typecheck、158 项服务端测试、完整 build、docs、navigation、UI/i18n、language-boundary 与浏览器验收全部通过。

#### 4.1.5 项目概述：Idea 与定期汇报反馈页面（旧）

- [x] `070-B1` 让规格字段清楚显示状态（模型候选、用户已确认、未解决）、来源、Idea 版本和差异；未确认字段要阻止下游检索、方法和实验。 [Apple 设计验收]

> 验收记录（2026-08-04）：`项目概述 -> Idea 讨论` 的规格字段现在逐字段显示状态徽标（用户已确认/模型候选/未解决）、来源（用户批准修订/项目创建时确认/模型候选草稿）、Idea 版本与修订差异（如 `v1 -> v2`），并补齐可用数据、伦理与合规、关键词等字段。服务端新增 `spec_field_status` 推导与 `spec_field_unconfirmed` 门禁：核心字段（研究问题、领域、可用数据、伦理与合规）未确认时，检索、相关工作递归、实验计划、论文草稿和编译返回结构化 409 并列出阻塞字段，前端同步显示提示并禁用对应按钮；Idea 修订白名单扩展到全部规格字段（数组字段支持 JSON 值），用户可在项目对话中确认任意规格字段。服务端新增 `spec-field-status` 测试（推导、门禁、放行、修订确认），`related-work-api` 夹具补齐完整规格；真实浏览器验收 16 个规格字段徽标、版本来源与差异行无横向溢出，Web 类型检查、构建、UI/i18n、navigation、language-boundary、docs 与完整服务端测试均通过（related-work-recovery 单跑通过，整套并行时因 PGlite 锁争用偶发超时）。
- [x] `070-B2` 创新点、边界、重复风险、反例和待核验问题必须绑定 Paper/Evidence/Claim/IdeaVersion，不能由模型直接宣布创新。 [Apple 设计验收]

> 验收记录（2026-08-04）：研究状态候选契约扩展为 `gap / cluster / duplicate_risk / innovation / boundary / counterexample / open_question` 七类，并在 `research_status_gap_candidates` 持久化 `paper_ids / evidence_ids / claim_review_ids / idea_version / basis`。创建候选时只允许引用当前项目已完成 ClaimReview 的矩阵行，来源必须能反查到 Paper、Evidence 与 ClaimReview，且 Idea 版本必须与矩阵一致；空行、跨项目行和过期 Idea 版本分别返回 422/403/409。`Idea 讨论` 的创新矩阵探索与相关工作的研究状态页都提供七类候选选择、来源计数、Paper/Evidence/ClaimReview 详情、页码定位链接与保留/拒绝操作；全部 UI 文案走四语言 i18n。服务端 `research-status.test.ts` 新增来源绑定与拒绝用例（6 项全过）；真实浏览器验收在 seeded matrix 上确认 7 个候选类型选项、1 条候选、来源计数/详情、2 个决定按钮且无横向溢出；Web/server typecheck、web build、完整 build、ui/i18n、navigation、docs 检查均通过。
- [x] `070-B3` 项目进度时间线要包含搜索、复现、Artifact、报告、反馈、失败、取消、暂停、审批和上游失效等事件。 [Apple 设计验收]

> 验收记录（2026-08-04）：`项目概述` 的“研究进度”时间线现在合并项目详情返回的最近 300 条审计事件，以及 Artifact（按 Run/实验分组）、报告、反馈、相关工作递归运行、复现运行、后台失败任务和检查点记录，覆盖搜索、复现、Artifact、报告、反馈、失败、取消、暂停/恢复、审批与上游失效等事件；过滤置顶/排序/设置等非进度审计噪音。每条事件使用按类型配色的 Apple 风格圆点、i18n 标题、结构化详情与状态徽标，并补充 `.badge.pending/.failed` 配色与移动端徽标换行；`/api/projects/:ref/audit` 同时修复为接受语义 slug 而非仅 UUID。真实浏览器验收时间线显示 14 条事件、8 种事件类别、时间戳与无横向溢出；新增 `project-detail-audit` 服务端测试断言详情返回审计事件且 slug 审计接口可用，Web 类型检查、构建、UI/i18n、navigation、language-boundary、docs 与相关服务端测试均通过（完整并行套件中 `related-work-recovery`/`impact-service` 偶发 PGlite 锁争用超时，二者单跑均通过）。
- [ ] `070-B4` Idea 讨论与常驻项目对话只通过 Mastra Agent/Skills/Tools/Workflow 实现，保存 session、run、档位、输入/输出 hash、结构化候选和用户决定；不复制固定问题队列，也不让模型直接写代码、配置、实验、报告和论文。 [Apple 设计验收]
- [x] `070-F2` 定期汇报与反馈页面独立展示，补齐日期/状态/来源筛选、段落来源抽屉和版本选择；不使用“日报/周报”作为前端命名。 [Apple 设计验收]
> 验收记录（2026-08-04）：报告页新增日期范围/状态/来源快照筛选和版本选择；报告生成写入 `paragraph_sources` 段落级来源映射，界面提供段落来源抽屉，旧报告无段落映射时如实显示“只有报告级来源快照”。前端将“日报/周报”改为“按日/按周汇报”，报告正文不再出现“导师反馈 / Waiting for mentor decision”。真实浏览器验证筛选栏、版本选择、段落来源 5 个条目与浅/暗色无横向溢出；服务端报告测试断言无角色化措辞且 `Observed activity`、`Waiting for decision` 段落来源正确，完整 36 个测试文件/151 个用例、Web 类型检查、构建、UI/i18n、navigation、language-boundary 和浏览器验收均通过。
- [x] `070-F5` 在报告/反馈页面清楚区分“已完成事实、结构化失败、等待决定、模型建议、未核验候选、外部阻塞”，并显示来源、权限和下一步。 [Apple 设计验收]
> 验收记录（2026-08-04）：报告页新增状态图例、项目范围标识、每条报告/反馈/审计的状态徽标、下一步说明，以及报告来源快照计数与缺失来源提示；真实浏览器验证图例 8 个类别、活动报告为 `Unverified candidate`、反馈为 `Waiting decision`、审计为 `Recorded`，来源行与项目范围正确，浅/暗色无横向溢出。Web 类型检查、构建、UI/i18n、navigation、language-boundary、服务端 36 个测试文件/151 个用例和完整浏览器验收均通过。

#### 4.1.6 相关工作调研链路（旧）

- [x] `070-C3` 递归搜索按 depth/width/max_total 分层执行，支持审批、稳定排序、去重、引用边、进度、取消、部分/失败和恢复；补独立进程重启 API 验收。 [Apple 设计验收]

> 验收记录（2026-08-04）：`related-work-core.test.ts` 覆盖 depth/width/max_total 分层、稳定去重、非悬挂引用边、进度回调、max_total 截断与取消；`related-work-api.test.ts` 覆盖审批门禁、递归运行与引用边持久化、跨项目拒绝；`related-work-recovery.test.ts` 覆盖 `running -> queued/partial` 与 `native_process_restarted` 标记。新增 `scripts/related-work-restart-acceptance.ts`（已注册为 `npm run related-work:restart:check`）启动真实 API 子进程两次，使用独立 `RESEARCH_RUNTIME_DIR` 验证重启前后同一个 `queued` 递归运行都能通过 `/api/projects/:slug` 读取且状态、ID、错误一致；临时运行时目录在脚本结束后清理。配合 `070-C9e` 的真实浏览器验收，`070-C3` 全部出口通过。
- [~] `070-C4` PDF 全文证据要保存稳定 URL、SHA-256、页码/章节、原文 quote 和证据状态；没有 quote 的元数据只能当候选，不能进入已确认矩阵。 [Apple 设计验收]
- [~] `070-C5` 字段来源冲突要按字段展示 provider、来源尝试、用户值、冲突组和人工决定；模型补全必须走 Proposal，失败不能覆盖已有成功字段。 [Apple 设计验收]
- [~] `070-C6` 研究现状矩阵只消费已确认 Paper、带页码/章节的 Evidence 和已接受 ClaimReview，保留 Idea 版本和来源快照；gap/聚类/重复风险只能是待核验候选。 [Apple 设计验收]
- [~] `070-C7` 引用图只投影数据库里明确保存的关系，节点分候选/Paper/Evidence/ClaimReview 层级，边显示关系、证据和权限；空、部分、失败、跨项目状态不能共用同一张静态图。 [Apple 设计验收]
- [~] `070-C8` 代码复现按“仓库发现 -> 固定 commit 下载 -> 依赖 Proposal -> 运行 Proposal -> 产物 Proposal”分步审批；复现代码永远放在 `experiment/reproductions/`，绝不进入方法代码。 [Apple 设计验收]
  - [~] `070-C8a` 只从 Paper 明确保存的 GitHub/GitLab URL 发现仓库，不从标题猜仓库；记录 locator 和来源。 [Apple 设计验收]
  - [~] `070-C8b` 验证 DOI/精确标题匹配、SPDX 许可证、默认分支、40 位 commit、入口、依赖、数据、系统/GPU 要求；任一未知就保持 candidate/blocked。 [Apple 设计验收]
  - [~] `070-C8c` 下载、依赖安装、运行、产物写入分别建立 Proposal；复核归档限制、路径穿越、链接文件、源码 hash、`.venv` 隔离和有界日志。 [Apple 设计验收]
  - [~] `070-C8d` 复现 Run 记录 source commit、入口、seed、配置、原始指标、日志、Artifact hash 和失败码；用与当前 topic 相关且用户允许的真实 Linux 仓库做端到端验收。 [Apple 设计验收]
- [x] `070-C9c` 缓存命中/过期、schema/请求哈希、失败不覆盖成功、项目隔离、审计和 running -> queued/partial 已测试；补同一候选多 provider provenance 和独立进程重启 API。 [Apple 设计验收]

> 验收记录（2026-08-04）：`related-work-cache.test.ts` 覆盖 miss/hit、TTL 过期、schema 不兼容、请求哈希、项目隔离、失败不覆盖成功、失败保持失败与审计；`related-work-recovery.test.ts` 覆盖 running -> queued/partial 与重启标记；`related-work-api.test.ts` 与 `scripts/seed-related-work-fixture.ts` 覆盖同一候选多 provider provenance（Crossref/OpenAlex 冲突、用户值、冲突组、人工选择）以及跨项目矩阵拒绝。`scripts/related-work-restart-acceptance.ts` 以独立 API 子进程验证重启前后递归运行状态与 ID 一致，脚本真实运行通过；浏览器验收 `provenanceDrawer.hasConflict/hasSelected` 为真且无横向溢出。所有出口均已关闭。
- [~] `070-C9d` seed/候选/Proposal/递归运行/人工确认/项目隔离 API 主链已测试；补 Artifact/证据跨项目矩阵和“失败可重试但不覆盖成功”的完整 fixture。 [Apple 设计验收]
- [ ] `070-C9f` 用当前 `.env` 的合法配置做真实来源验收；失败如实记录为外部阻塞，不使用旧缓存、不换 provider、不做 fallback。 [Apple 设计验收]

#### 4.1.7 本方法实现：方法代码与实验（旧）

- [~] `070-D` 项目代码目录和受限文件树已有基础，但 Git/备份/Proposal 闭环未完成；相关页面位于 `实验实现 -> 本方法实现`。 [Apple 设计验收]
  - [ ] `070-D1` 方法设计只消费已确认规格、已审阅证据和允许的复现报告，输出结构化候选、依赖、数据契约、风险、假设和实验建议，并保存输入 ID、Mastra run 和版本 hash。 [Apple 设计验收]
  - [ ] `070-D2` 完善代码详情、受限 diff、依赖变更、测试结果和 Git 状态；只允许项目范围内白名单文件类型和相对路径，拒绝 shell、SQL、绝对路径、网络目标和可执行文件注入。 [Apple 设计验收]
  - [ ] `070-D3` 写文件、配置、依赖、branch、commit、remote sync、push、冲突和恢复分别建立 Proposal/审计，包含 diff、影响、成本、撤销和审批。 [Apple 设计验收]
  - [ ] `070-D4` 备份代码、配置模板、Git metadata、Artifact metadata 和 SHA-256；恢复先在隔离目录验证，不能包含 `.env`、key、Cookie、数据库原文件或认证材料。 [Apple 设计验收]
  - [ ] `070-D5` UI 明确区分外部复现代码和我们的方法代码的物理目录、branch、commit、来源和 lineage；未审批/未测试/未 commit 的代码不得进入实验。 [Apple 设计验收]
  - [ ] `070-D6` 通过双项目 containment、恶意文件名、超大 diff、符号链接、依赖篡改、remote 凭据泄漏、恢复冲突和重复 commit 测试。 [Apple 设计验收]
- [ ] `070-E` 实验结果闭环尚未完成；实验计划、运行队列、指标、结果预览和谱系作为所选实验的详情能力，统一位于 `实验实现 -> 本方法实现/相关工作实现` 的右侧工作区，不再拆成第三级标签，也不得出现在 `学术论文撰写`。 [Apple 设计验收]
  - [ ] `070-E1` 实验计划固定实验类型、当前 topic 问题/假设、入口、项目 `.venv`、seed、IdeaVersion、方法 commit、数据版本、配置指纹、资源限制和审批人；缺字段不得入队。 [Apple 设计验收]
  - [ ] `070-E2` TypeScript supervisor 管理固定入口、队列、进程树取消、超时、暂停/恢复、有界日志、资源和失败状态；mean/std/置信区间只能由程序根据真实 seed 计算。 [Apple 设计验收]
  - [ ] `070-E3` Artifact 预览覆盖 PNG/JPEG、Markdown/JSON/CSV、loss/时序、PLY/点云、mesh/PDF；校验 containment、大小、MIME/魔数、hash、Run 和撤销状态。 [Apple 设计验收]
  - [ ] `070-E4` 结果回链 IdeaVersion、方法 commit、数据、配置、Run、seed、日志和 Artifact；失败/取消/无效产物不得进入报告或论文。GPU/CUDA 真实验收受 `P0-RUNNER-007` 阻塞时保持 `[!]`。 [Apple 设计验收]
  - [ ] `070-E5` 所选实验的详情工作区清楚分组展示计划、运行、指标、产物和 lineage，支持多 seed、缺失/失败 seed、partial/cancelled；禁止无关 baseline、预置图表、空数组成功和任何 fallback。 [Apple 设计验收]
  - [ ] `070-E6` 通过重复确定性、数值精度、异常指标、超大产物、符号链接、错误扩展名、跨项目 Artifact 和上游失效递归测试。 [Apple 设计验收]

#### 4.1.8 项目概述：定期汇报与反馈闭环（旧）

- [~] `070-F1` 报告按时间窗口读取真实对话、审计、任务、实验、Proposal、来源尝试和反馈，保存 event_count、时间窗口、cutoff、来源 ID、失败/阻塞和证据边界；补 Mastra report run 和完整 source snapshot（段落 provenance 已由 `070-F2` 完成）。 [Apple 设计验收]
- [x] `070-F3` 反馈只能创建 feedback/Proposal；支持逐条确认、拒绝、要求修订、指派上游实体、状态变更和审计，不能直接触发代码、依赖、实验或 Git。 [Apple 设计验收]

> 验收记录（2026-08-04）：`/api/projects/:id/feedback` 只写入 `human_feedback` 并创建审计；`/feedback/:id/decision` 支持 `acknowledged / rejected / revision_requested`，`/feedback/:id/proposal` 只创建 `pending` Proposal，拒绝后的反馈不能创建动作，跨项目决策返回 404；反馈携带 `reference_id` 指派上游报告/实体，Proposal payload 保留 `feedback_id / feedback_reference_id / feedback_instruction`。`反馈与汇报` 页面提供逐条确认、拒绝、要求修订、创建 Proposal 按钮，并为每条反馈显示状态徽标、下一步和决策说明。真实浏览器验收显示反馈行 1 条 `Waiting decision`、操作按钮 4 个、包含“Create Proposal”动作且无横向溢出；`feedback-api` 2 项、`reports-api` 2 项、`report-lineage` 4 项服务端测试通过，Web 类型检查、构建、UI/i18n、navigation 与完整浏览器验收通过。
- [x] `070-F4` Markdown 预览使用受控渲染器，保留标题、表格、代码、图片和上游链接；历史版本不可变，重试生成新版本。 [Apple 设计验收]

> 验收记录（2026-08-04）：Markdown 解析与渲染拆分为 `apps/web/src/markdownParser.ts` 和 `MarkdownPreview.tsx`，只允许 `https://` 与 `/api/` 图片源和 `https://` 链接，表格解析会跳过分隔行，代码块、标题、列表、引用、表格、图片与上游链接均保留；`javascript:` 等不安全图片源不会被识别为图片。新增 `scripts/check-markdown-preview.ts` 并纳入 `npm run ui:check`，断言标题、带表头的 2 列表格、HTTPS 图片、代码块和上游链接均正确解析。真实浏览器验收通过 fetch 注入带表格/图片/链接的报告内容，确认 `.markdown-preview table`、惰性加载图片与外部链接渲染正常且无横向溢出，截图 `108h-markdown-preview.png`；报告生成每次写入新版本，历史版本可回看，重试生成不会覆盖旧版本。Web 类型检查、构建、UI/i18n、navigation 与完整浏览器验收通过。
- [~] `070-F6` 报告读取侧校验 source snapshot、项目范围、实体存在性和 Artifact 有效性；跨项目/缺失来源标记为 `blocked`，只渲染有效 Markdown；事件驱动生成、反馈引用和完整复现 lineage 仍待完成。 [Apple 设计验收]

#### 4.1.9 学术论文撰写（旧）

- [ ] `070-G` 学术论文撰写与 Linux 编译闭环尚未完成；页面按 `引言 -> 相关工作 -> 方法介绍 -> 实验 -> 结论` 五个内容章节组织，引用、BibTeX、图表、实验数据、LaTeX 编译和 PDF 是章节工作区内的能力，不再作为顶层标签，也不承载实验管理。 [Apple 设计验收]
  - [ ] `070-G1` 论文、五类章节、协作记录、写作 Proposal、修订记录、AI disclosure 和阶段门禁结构化；章节不可变，修改产生 diff，并为每个英文句子保存界面专用的对应中文译文。 [Apple 设计验收]
  - [ ] `070-G2` Markdown/LaTeX preview 保留 Paper/Evidence/Claim/BibTeX/page quote 关联；引用面板拒绝 metadata-only 或无 locator Evidence。 [Apple 设计验收]
  - [ ] `070-G3` 只能选择 lineage 完整且有效的 Experiment/Artifact 插入图片和数字，保留 caption、指标来源、commit、数据版本、配置指纹和 Run ID。 [Apple 设计验收]
  - [ ] `070-G4` Linux `latexmk` 使用隔离目录、固定入口、有界日志、PDF Artifact、编译 hash 和失败诊断；不在 Windows 原生侧启动编译服务。 [Apple 设计验收]
  - [ ] `070-G5` 最终门禁检查引用、证据、图表、实验、数据、上游有效性、AI disclosure、用户反馈和未决 Proposal；有缺口就阻止 PDF 交付，中文辅助译文不得写入最终 PDF。 [Apple 设计验收]
  - [ ] `070-G6` 章节、引用、图表和实验都能回链上游；Paper/Evidence/Run/Artifact 失效时标记受影响段落并阻止编译。 [Apple 设计验收]
  - [ ] `070-G7` 验收模型失败不产生伪段落、BibTeX key 稳定、引用不重复、图片路径 containment、编译失败无成功状态、跨项目拒绝和历史版本可回看。 [Apple 设计验收]

#### 4.1.10 端到端（旧）

- [ ] `070-H` 四个一级入口下的真实端到端数据流、状态/权限/失败关闭、项目隔离、键盘导航、桌面/移动截图、真实来源、实验、定期汇报和论文门禁全部通过；不以静态占位页、fixture 数字、旧缓存或无关 baseline 替代真实链路。 [Apple 设计验收]

#### 4.1.11 文档合同收尾（旧）

- [~] `P0-DOC-RESEARCH-076` 文档合同主体已建立，剩余是逐项对照和同步。 [Apple 设计验收]
  - [ ] `076d` 用真实 React 路由/tab、API、数据库实体、测试和浏览器截图逐项对照合同；页面能打开或 fixture 通过不能单独关闭。 [Apple 设计验收]
  - [ ] `076e` 下游业务实现完成后，同步 `AGENTS.md`、`README.md`、`README.zh-CN.md`、架构/安全/运维文档和 `DOCS_SYNC_VERSION`。 [Apple 设计验收]
  - [ ] `076f` 运行适用类型检查、测试、构建、文档检查、语言边界检查和浏览器验收；真实外部阻塞保持 `[!]`。 [Apple 设计验收]

#### 4.1.12 模型与 Supermemory 收尾（旧）

- [~] `P0-MODEL-API-101` 自有请求已确认不再触发上游 `json_object` 报错，剩余是外部面板对账和 Supermemory 内部请求确认。 [Apple 设计验收]
  - [~] `101d` 重启全局 Supermemory 和 embedding pool 后，运行实例的 `OPENAI_BASE_URL` 已核对为 `http://127.0.0.1:3010/v1`；下一步在有可处理的真实文档/LLM-agent 队列时，确认 bridge 收到的原始请求形状，验证 Supermemory 内部请求确实走本地 bridge。 [Apple 设计验收]
  - [!] `101b` 上游控制面板告警的最终来源需要 request-id 才能与当前进程逐条对应；当前仓库无法读取该控制面板，等待用户提供 request-id。 [Apple 设计验收]
- [~] `P0-MASTRA-050` Agent/Memory/Skills/Tools/Workflows/Approval 统一使用 Mastra；材料索引和真实 provider 验收仍需外部条件。接入新 Mastra API 前先核对 `https://mastra.ai/llms.txt`、官方文档和当前安装版本类型定义。 [Apple 设计验收]
- [~] `P1-UPLOAD-009` 大规模材料索引、项目范围语义检索和失败重放；PDF/图片终态仍受 Supermemory 外部条件阻塞。 [Apple 设计验收]
- [~] `P2-HA-021` 长期无人值守、外部告警、备份恢复和跨重启演练尚未完成。 [Apple 设计验收]

## 5. 外部阻塞（等待外部条件，不阻塞其它排期）

- [!] `P0-SUPERMEMORY-LOCAL-053` 当前 Supermemory server build 的远程 embedding/PDF/图片能力存在已验证限制；保持失败关闭。解除条件：受支持的 Linux build/源码补丁和可用模型端点完成真实验收，或用户明确批准新的兼容实现。 [Apple 设计验收]
- [x] `P0-RUNTIME-101` 2026-08-03 用户明确同意直接使用已验证的恢复候选库（测试阶段，无真实科研数据），`.env` 的 `RESEARCH_RUNTIME_DIR` 已切到 `runtime/restore-pglite-model-check-20260803`，8080 API 健康检查、语音设置与 Groq 真实转写均已通过。 [Apple 设计验收]
- [!] `P0-RUNNER-007` 目标 WSL2 主机的真实 GPU/CUDA 资源尚未验收；CPU supervisor 测试不能代表 GPU 通过。 [Apple 设计验收]
- [!] `P0-DEPS-056` 当前 Mastra/deployer 依赖链的上游 Hono advisory 需等待兼容修复；不能用破坏性降级掩盖。 [Apple 设计验收]
- [x] `DOC-MODEL-107` 默认文档文本模型 `deepseek-v4-flash` 在固定网关 `http://127.0.0.1:3000/v1` 返回 403（`The latest version of this model is only available hosted in China and requires explicit opt in`）。已确认为上游模型/网关 RegionError，不是请求格式问题；已通过运行时模型设置配置网关可用的 `grok-4.5` 解除，并用真实 `/api/chat`、`paper-translate`、`paper-revise` 请求完成端到端验收。 [Apple 设计验收]

## 6. 执行顺序

1. 当前先严格执行 `P0-WORKSPACE-108A -> 108B -> 108C -> 108D -> 108E -> 108F -> 108G -> 108H`；这是用户明确规定的最高优先级顺序，旧任务池中的任务不得插队，也不能把旧页面补丁当作新架构完成。
2. 每一步先完成用户能直接操作的页面和交互，再接入该页面所需的真实 API、状态、审批、项目隔离和测试；不能交付静态 mock，也不能为了先做后台而长期留下空壳 GUI。
3. `108H` 完成后，重新审计 `4.1` 旧任务池：已被新工作台覆盖的条目按真实验收关闭，仍有独立价值的缺口再重新排期。
4. 旧任务池中的后台支撑、文档同步和平台稳定性任务只在当前 `108*` 步骤确实依赖时穿插推进；任何外部阻塞保持 `[!]` 并写清解除条件，不得用 fallback 跳过。
5. 最终全链路验收以 `108H` 为准；旧任务池不再自动获得优先级，也不能代替新工作台验收。

每次开始非微小工作前先把对应任务标为 `[~]`；范围、依赖、阻塞改变时立即更新；只有真实出口全部通过才能标为 `[x]`。验收失败保持 `[~]` 或 `[!]`，不得提交或 push。

## 7. 必跑验证

应用、数据库、运维和测试使用 TypeScript；科研 Python 例外只在项目实验/复现目录。适用命令：

```text
npm run typecheck
npm test
npm run build
npm run idea-cases:check
npm run docs:check
npm run language-boundary:check
npm run supermemory:acceptance
npx tsx scripts/acceptance-test.ts
npx tsx scripts/ops-guard.ts status
npm run mastra:hitl:check
npm run mastra:evals:check
npm run model-failure:check
npm run experiment:check
```

相关工作改动至少还要运行：

```text
npm run typecheck --workspace @research-os/server
npm run typecheck --workspace @research-os/web
npm test --workspace @research-os/server -- related-work
```

真实 provider、模型、Supermemory、GPU、LaTeX 或外部告警不可用时，记录结构化错误、复现步骤和解除条件；不得用 fixture 通过冒充真实验收。
