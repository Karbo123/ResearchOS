# 变更审批

- 项目：`{{ project_id }}`
- 提案：`{{ proposal_id }}`
- 类型：`{{ kind }}`
- 提出者与时间：`{{ actor }}` / `{{ created_at }}`
- 修改原因：`{{ reason }}`
- 修改摘要：`{{ summary }}`
- Git/config diff：`{{ diff }}`
- 预计资源与 API 成本：`{{ estimated_cost }}`
- 将重跑：`{{ rerun_experiments }}`
- 将失效：`{{ invalidated_metrics_artifacts_claims }}`
- 风险与回滚点：`{{ risks_and_checkpoint }}`

决定：`approved | rejected`
批复人：`{{ reviewer }}`
意见：`{{ comment }}`
