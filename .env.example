# Research OS local configuration template.
# Copy this file to .env, replace every placeholder, and never commit .env.
# The native TypeScript app persists embedded PostgreSQL-compatible state and
# controlled artifacts under these repository-relative directories.
RESEARCH_RUNTIME_DIR=runtime
RESEARCH_PROJECTS_DIR=projects
RESEARCH_ARTIFACTS_DIR=artifacts
RESEARCH_API_PORT=8080
MASTRA_BASE_URL=http://127.0.0.1:4111
MASTRA_STUDIO_URL=http://127.0.0.1:4111

# The in-process durable queue leases and dispatches only project-scoped workflows.
QUEUE_POLL_SECONDS=2
QUEUE_LEASE_SECONDS=180
QUEUE_WORKFLOW_TIMEOUT_SECONDS=90
QUEUE_RETRY_BASE_SECONDS=5
QUEUE_RETRY_MAX_SECONDS=300
RESEARCH_OS_COMMIT=unavailable

# API report scheduler dispatches one project workflow per active project at the
# configured local times. The state file prevents repeated dispatches in the
# same daily/weekly window even when the server restarts.
RESEARCH_REPORT_POLL_SECONDS=30
RESEARCH_REPORT_DAILY_TIME=09:00
RESEARCH_REPORT_WEEKLY_TIME=09:30
RESEARCH_REPORT_WEEKDAY=1
RESEARCH_REPORT_TIMEOUT_SECONDS=180

# Project workflow loader polls projects/*/workflow.ts for changes. /mnt/d has
# no inotify, so hot reload is hash polling; loader/runtime code changes still
# require restarting Mastra.
RESEARCH_WORKFLOW_POLL_INTERVAL_MS=500

# Native experiment execution. Each Python project gets projects/<id>/.venv.
# The Linux backend (WSL2 native, default when the service runs in WSL2) uses
# python3; an explicitly selected Windows host backend uses python.exe.
RESEARCH_PYTHON_EXECUTABLE=python3
EXPERIMENT_TIMEOUT_SECONDS=3600

# The native Mastra process calls the configured OpenAI-compatible Responses API.
# These model URLs are operation-free bases; the provider appends /responses.
# A failed request is returned as an API error; there is no provider switch,
# local reply, or unrelated experiment generation.
MODEL_REQUEST_TIMEOUT_SECONDS=240

# Project-scoped semantic memory through the locally hosted Supermemory server.
# Localhost requests may use Supermemory Local's automatic local authentication.
# Put an explicit local key here when using a non-loopback address or disabling
# automatic localhost authentication. All failures remain fail-closed.
SUPERMEMORY_ENABLED=true
SUPERMEMORY_BASE_URL=http://127.0.0.1:6767
SUPERMEMORY_API_KEY=
SUPERMEMORY_LOCAL_AUTO_AUTH=true
SUPERMEMORY_TIMEOUT_MS=30000
# Path to the official self-hosted supermemory-server executable used by
# `npm run supermemory:start`; leave empty when the service is managed outside
# this repository. Inside WSL2 this is a Linux path such as
# /home/<user>/bin/supermemory-server-linux-x64 (the server-v0.0.5 release
# asset; server-v0.0.6+ and 0.0.7-rc.2 regressed remote embedding).
SUPERMEMORY_SERVER_BIN=

# The current Supermemory Local binary may emit Chat Completions requests for
# its internal memory agent. Research OS starts a loopback-only TypeScript bridge
# so the fixed model gateway receives only POST /responses. The bridge uses the
# medium Responses model settings below as its target; keep it enabled unless a
# Supermemory build is independently verified to use Responses itself.
SUPERMEMORY_MODEL_BRIDGE_ENABLED=true
SUPERMEMORY_MODEL_BRIDGE_PORT=3010
SUPERMEMORY_MODEL_BRIDGE_TIMEOUT_MS=120000

# Embedding provider for Supermemory Local. Official self-hosted docs define
# SUPERMEMORY_EMBEDDING_PROVIDER/MODEL/DIMENSIONS/BASE_URL/API_KEY. The local
# default is the multilingual ONNX model Xenova/bge-m3 (1024 dims). Remote
# embedding (OpenAI-compatible, e.g. Qwen3-Embedding-8B with
# SUPERMEMORY_EMBEDDING_BASE_URL=https://ai.gitee.com/v1 and an API key) is
# implemented by the server-v0.0.5 build only; 0.0.6+ and 0.0.7-rc.2 ship just
# the local ONNX worker. Requesting remote embedding against an unsupported
# build fails closed (start script version guard + API guard) instead of
# silently using local vectors. Changing model or dimensions requires a fresh
# data directory or a full re-ingestion. Remote Qwen3-Embedding-8B supports
# 32-4096d, but the server's pgvector HNSW store fails chunk upsert above
# ~1024d (isolated tests: 1024d OK, 1536d/2000d fail), so 1024d is the verified
# working configuration. The 2000d request is blocked by this upstream limit
# (see TODO 053-C).
# These variables are the GLOBAL default. Each research project may override
# them through the web UI (left-bottom settings → Embedding). Projects with
# identical provider/model/dimensions/base_url/key share one configuration-pool
# instance (port 6770–6869, data under runtime/supermemory/pools/<pool-key>/data,
# registry runtime/embedding-pools.json); project isolation inside a pool is
# enforced by Supermemory container tags. Per-project settings are stored in
# runtime/project-embedding-settings.json (mode 0600); switching model/dimensions
# rebuilds the pool's data directory (old data is backed up and must be re-ingested).
SUPERMEMORY_EMBEDDING_PROVIDER=local
SUPERMEMORY_EMBEDDING_MODEL=Xenova/bge-m3
SUPERMEMORY_EMBEDDING_DIMENSIONS=1024
SUPERMEMORY_EMBEDDING_BASE_URL=
SUPERMEMORY_EMBEDDING_API_KEY=

# Optional explicit proxy for Supermemory child processes (global instance and
# pool instances). The runtime deliberately does NOT forward HTTP(S)_PROXY from
# the host shell: a stale WSL NAT proxy address (e.g. an old /etc/resolv.conf
# host IP captured by a set_proxy.sh script) makes remote embedding and
# HuggingFace model downloads fail. Leave empty for direct connections.
SUPERMEMORY_PROXY_URL=

# Each tier is independent: model, Responses base URL, key, and reasoning effort.
# Do not put /responses, /chat/completions, or /completions in these URLs. Plain
# HTTP is accepted only for loopback and RFC1918 private addresses.
# Non-empty web settings override these values; empty legacy runtime fields
# inherit the project .env defaults. Keys are written to ignored runtime/
# model-settings.json and never returned by the API.
RESEARCH_MODEL_SIMPLE=gpt-5.6-luna
RESEARCH_MODEL_URL_SIMPLE=http://127.0.0.1:3000/v1
RESEARCH_MODEL_KEY_SIMPLE=
RESEARCH_REASONING_SIMPLE=low
RESEARCH_MODEL_MEDIUM=gpt-5.6-terra
RESEARCH_MODEL_URL_MEDIUM=http://127.0.0.1:3000/v1
RESEARCH_MODEL_KEY_MEDIUM=
RESEARCH_REASONING_MEDIUM=medium
RESEARCH_MODEL_COMPLEX=gpt-5.6-sol
RESEARCH_MODEL_URL_COMPLEX=http://127.0.0.1:3000/v1
RESEARCH_MODEL_KEY_COMPLEX=
RESEARCH_REASONING_COMPLEX=high
# Document text model: generates readable chat explanations and document-style
# text. An empty key falls back to the medium model key so the local gateway
# remains usable out of the box.
RESEARCH_DOCUMENT_MODEL=deepseek-v4-flash
RESEARCH_DOCUMENT_MODEL_URL=http://127.0.0.1:3000/v1
RESEARCH_DOCUMENT_MODEL_KEY=
# Image recognition model: used when chat or Idea discussion contains image
# attachments. It is a Responses API model like the code tiers above; an empty
# key falls back to the medium model key so the local gateway stays usable.
RESEARCH_VISION_MODEL=mimo-v2.5
RESEARCH_VISION_MODEL_URL=http://10.31.107.77:3000/v1
RESEARCH_VISION_MODEL_KEY=
# Image generation model through an /images/generations compatible endpoint.
# The web UI can override model, URL, key, resolution, and quality; the key is
# stored only in ignored runtime/model-settings.json and never returned by the API.
RESEARCH_IMAGE_MODEL=gpt-image-2-official
RESEARCH_IMAGE_MODEL_URL=https://api.apimart.ai/v1
RESEARCH_IMAGE_MODEL_KEY=
RESEARCH_IMAGE_RESOLUTION=1k
RESEARCH_IMAGE_QUALITY=low
RESEARCH_ROUTER_SIMPLE_MAX=2
RESEARCH_ROUTER_MEDIUM_MAX=7

# Optional academic/code-provider credentials. Empty values use public quotas.
# GitHub results remain unverified candidates until source/license cross-checking.
GITHUB_TOKEN=
GITLAB_TOKEN=
SEMANTIC_SCHOLAR_API_KEY=

# Polite-pool contact for Unpaywall (no messages are sent; only used as the
# email query parameter) and the mailto identifier sent to Crossref/OpenAlex.
UNPAYWALL_EMAIL=research-os@users.noreply.github.com
RESEARCH_CONTACT_EMAIL=

# Project-scoped related-work provider response cache. The cache key includes
# project, provider, operation, query parameters, and the current schema version.
# A hit replays only the stored structured response; failures never become success.
RESEARCH_RELATED_WORK_CACHE_TTL_SECONDS=86400

# Mastra daily/weekly workflow schedule timezone.
REPORT_TIMEZONE=Asia/Shanghai

# Optional bounded health monitoring. HTTP alert URLs must be loopback/private;
# HTTPS alert URLs may be used for an explicitly configured external receiver.
RESEARCH_MONITOR_INTERVAL_SECONDS=30
RESEARCH_ALERT_WEBHOOK_URL=

# Upload security: the API invokes the fixed Windows Defender scanner through
# the native host path (Windows) or the WSL interop mount (/mnt/c) and fails
# closed when the scanner is unavailable, reports a threat, or times out.
WINDOWS_DEFENDER_EXECUTABLE=
MALWARE_SCAN_TIMEOUT_SECONDS=30
MATERIAL_MAX_FILE_BYTES=52428800
MATERIAL_MAX_SESSION_FILES=50
MATERIAL_MAX_SESSION_BYTES=262144000
MATERIAL_MAX_PROJECT_FILES=200
MATERIAL_MAX_PROJECT_BYTES=2147483648

# Voice recognition: choose browser (built-in Web Speech) or api (any OpenAI
# /audio/transcriptions compatible endpoint, including Groq). The web UI can
# override the provider, model, base URL, and key; a runtime-provided key is
# stored in runtime/voice-settings.json (0600) and never returned by the API.
# Leaving the key blank in the UI keeps the existing key. A base URL must use
# HTTPS or loopback/private HTTP.
# Model and voice transcription traffic follows the global proxy setting in the
# General settings UI (runtime/model-settings.json). When enabled all external
# model traffic goes through the configured proxy; when disabled it connects
# directly. The default is derived from HTTPS_PROXY/HTTP_PROXY (or lowercase)
# at startup for compatibility, so a local proxy can be used when the voice API
# is not directly reachable.
RESEARCH_VOICE_PROVIDER=browser
RESEARCH_VOICE_API_MODEL=whisper-large-v3-turbo
RESEARCH_VOICE_API_URL=https://api.groq.com/openai/v1
RESEARCH_VOICE_API_KEY=
RESEARCH_VOICE_GROQ_MODEL=whisper-large-v3-turbo
RESEARCH_VOICE_GROQ_URL=https://api.groq.com/openai/v1
GROQ_API_KEY=
